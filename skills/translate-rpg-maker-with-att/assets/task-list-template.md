# ATT 翻译任务：<engine>/<项目名>

## 整体方案

<只读了解整个项目后，用简洁文字说明用户目标、提取—翻译—写回的路线、主要选择、风险和
验证方法。>

## 任务信息

- 创建时间：<YYYY-MM-DD HH:mm:ss 时区>
- 最后更新时间：<YYYY-MM-DD HH:mm:ss 时区>
- 任务清单：<本文件绝对路径>
- 任务材料根：<本次实际采用的任务根绝对路径>
- 游戏来源与实际加载位置：<原件、隔离副本或实际消费者>
- engine：<mv | mz>
- 项目名：<ATT 项目名>
- ATT 项目工作区：<绝对路径>
- 来源身份：<游戏版本、补丁或 MOD；ATT 可用时记录来源指纹>
- ATT 程序：<att.exe 绝对路径；--version；SHA-256>
- ATT 发布目录：<绝对路径>
- 调用 cwd：<绝对路径>
- 显式配置：<绝对路径；SHA-256>
- 实际 `projects.root`：<绝对路径>
- 实际 `prompts.root`：<本次使用 Translate 时填写绝对路径>
- 本次已读取产品文档：<发布目录下的相对路径；进入新阶段时追加>
- 用户目标：<可观察结果>
- 范围：<包括什么，做到哪里停止>
- 必须保持：<不能被翻译工作改变的行为>
- 最终输出：<预期位置和形式>
- 授权：
  - 项目内可恢复修改：<允许或不允许的范围>
  - 模型、外部服务和费用：<允许或不允许的范围>
  - 写回和目标替换：<允许或不允许的范围>
  - Lua 受管 API 外副作用：<允许的外部读取或执行、文件修改、进程、网络和私有 SQLite
    的具体范围与禁止项>
- 待用户决定：<没有则写“无”>

## 本次有效资源

<!-- 按阶段和实际需要填写。不要复制配置正文、凭据或模型请求正文。 -->

- Extract owner 与来源：<Builtin / Rules / Lua；显式或保存状态；采用文件及 SHA-256>
- Translate：<Profile；Terminology、Placeholder、Prompt、Lua 的实际来源及 SHA-256>
- WriteBack：<Standard-only 或 Lua；实际来源及 SHA-256>
- Lua 解析环境：<阶段或独立入口；主程序解析目录；调用 cwd；实际采用的路径来源
  （`LUA_PATH_5_4`、后备 `LUA_PATH` 或内置默认）；展开后的初始 `package.path`；脚本对
  `package.path`、`package.preload[name]`、`package.loaded[name]` 或 `package.searchers`
  的相关修改；`os.getenv` 使用的其他变量名、按现行敏感信息规定处理的实际采用值及其
  影响；没有 Lua 则写“无”>
- Lua 动态依赖与直接访问：<访问方式；最终绝对路径或生产者身份；SHA-256、可用性、动作
  和执行前后状态；逐项记录较多时填写 evidence 清单路径及 SHA-256；没有则写“无”>

## 当前状态

- 任务状态：进行中
- 当前阶段：<提取 | 翻译 | 写回>
- 活动 TODO：<一个或多个任务内唯一 ID>
- 最后权威状态：<当前数据库、冻结来源或发布终态>
- 恢复点：<中断后先核对什么，从哪一步继续>
- 下一步：<紧接着可以执行的工作>
- 阻塞：无

## 提取

完成条件：范围内玩家可见文本的来源、翻译单位、上下文、稳定身份和写回映射已经确认；
遗漏与排除有解释；提取结果可以重复得到。

<!--
需要 Init 时作为本阶段 TODO，不增加 Init 阶段。只填写当前真实工作，例如：

- [ ] EX-01 <具体工作>
  - 完成标准：<能够观察和检查的结果>
-->

## 翻译

完成条件：范围内译文经过 ATT 验收并确认保存，没有未解释的剩余内容、拒绝、质量问题或
不确定提交；人工质量检查范围和未审内容已记录。

<!-- 尚未进入时不预建 TODO；进入后根据最新项目事实填写。 -->

## 写回

完成条件：译文进入正确位置，候选结构与完整差异正确，发布终态已确认，并按用户要求在
实际加载位置完成检查。

<!-- 尚未进入时不预建 TODO；进入后根据最新项目事实填写。 -->

## 重要决定

- 无。

<!--
只追加会改变目标、范围、输入、授权、配置或既有验收结论的决定。已经失效的完成记录仍
保留，在相应阶段增加复核 TODO。
-->

## 交付记录

### <YYYY-MM-DD HH:mm:ss 时区>

- 状态：未完成
- 本次范围：<本轮处理的范围>
- 输出：待完成
- 验证：待完成
- 未完成或未验证内容：待完成
- 剩余风险：待确认

<!-- 同一交付返修时保留旧日期段，追加新记录，不覆盖历史结论。 -->

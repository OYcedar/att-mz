# 思考输出要求

在最终 JSON 之前先且仅输出一组 `<why>...</why>`。

- 响应必须直接以精确的小写无属性 `<why>` 开始，并以 `</why>` 结束。
- 内容经 Unicode trim 后必须非空，并针对每个 `[ID]` 分析语境、指代、人称、语气、术语、
  换行、ATT token、源语残留和最终格式。
- 不得只写“已检查”，不得把最终 JSON 放进 `<why>`。
- `</why>` 后只允许空白，然后直接输出 system Prompt 规定的 JSON。

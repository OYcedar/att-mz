# 思考要求

动手之前先想清楚：先输出一组 `<why>...</why>`，再给出 JSON。

- 回复直接以小写、无属性的 `<why>` 开始，到 `</why>` 结束，整组只出现一次。
- 里面对每个 `[ID]` 写下实际分析：语境、指代、人称、语气、术语、换行、保护
  标记、源语残留和最终格式；去掉首尾空白后仍有实在内容。
- 写出具体判断，而不是一句"已检查"；JSON 始终待在 `<why>` 之外。
- `</why>` 之后只留空白，然后是规定格式的 JSON。

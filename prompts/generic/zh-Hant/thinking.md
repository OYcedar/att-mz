# 先想清楚再下筆

在最終 JSON 之前，先想一想：輸出一組 `<why>...</why>`，然後才是 JSON。

- 回應直接以小寫、無屬性的 `<why>` 開頭，並以 `</why>` 收尾；這一組恰好出現一次。
- 在其中寫下你對每個 `[ID]` 的實際分析：語境、指代、人稱、語氣、術語、換行、
  受保護標記、來源語言殘留和最終格式。去除前後空白之後，必須留有實質內容。
- 給出具體推理，而不是一句空泛的「已檢查」；JSON 永遠放在 `<why>` 之外。
- `</why>` 之後只允許空白，然後是符合規定格式的 JSON。

# 思考輸出要求

在最終 JSON 之前先且僅輸出一組 `<why>...</why>`。

- 回應必須直接以精確的小寫無屬性 `<why>` 開始，並以 `</why>` 結束。
- 內容經 Unicode trim 後必須非空，並針對每個 `[ID]` 分析語境、指代、人稱、語氣、術語、
  換行、ATT token、來源語言殘留和最終格式。
- 不得只寫「已檢查」，不得把最終 JSON 放進 `<why>`。
- `</why>` 後只允許空白，然後直接輸出 system Prompt 規定的 JSON。

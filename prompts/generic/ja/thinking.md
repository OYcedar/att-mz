# 思考出力要件

最終 JSON の前に、一組だけの `<why>...</why>` を出力してください。

- 応答は属性のない正確な小文字 `<why>` で始め、`</why>` で終えます。
- Unicode trim 後の内容を空にせず、各 `[ID]` の文脈、指示対象、人称、口調、用語、改行、
  ATT token、原言語の残存、最終形式を具体的に検討します。
- 「確認済み」だけで済ませず、最終 JSON を `<why>` の中に入れません。
- `</why>` の後は空白だけを許し、続けて system Prompt が指定する JSON を出力します。

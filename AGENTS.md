# AGENTS.md

本文件记录项目长期有效的产品方向、架构判断规则，以及知识的归属与同步要求，让每一次开发都服务同一个产品目标。

这里只保存判断原则与知识分工；当前命令、参数、模块名、数据字段、第三方库、完成状态和具体工作流各有自己的家。现行规格描述已经确认的外部契约，代码与测试描述已经实现的事实。

每条规则都直接说明：遇到什么情况、如何判断、采取什么行动。对话中的具体例子用来帮助识别职责、不变量和取舍；写进现行规格的例子才成为通用规则。本文件用正面标准说明该怎么做，而不是罗列历史禁令。

> 总纲：从真实用户结果反推纵向切片，从当前职责推导直接依赖；允许跨越物理层级，但要经过负责相应规则、状态和不变量的模块（下文称“语义所有者”）；只设计当前目标及其直接依赖，根据已有调用方的实际需要复用模块和演进数据模型。

## 1. 产品形态：统一入口，独立纵向切片

ATT 用一个 CLI 聚合多个游戏引擎的翻译能力，每个引擎拥有彼此独立的命令域和纵向切片。统一入口只承担真正跨引擎一致的进程级职责和路由；每个引擎完整拥有自己的命令、参数、行为、错误语义和演进节奏。

表面相同的命令或流程，各自仍是独立的契约。各引擎先准确表达自己的用户意图，再在契约之下复用语义一致的能力。

面向用户的入口只表达本次操作意图，以及确实需要当场选择的事项；已经由项目状态或执行配置确定的事实直接复用。统一配置只收纳经过证据确认、确实属于部署环境、外部服务或操作者长期选择的策略。固定的产品语义、内部资源参数、调度细节和运行时数据由负责执行它们的模块决定——它们既不是每次操作都要重复填写的参数，也不伪装成用户配置。

```text
[统一产品入口]
       |
       +----> [引擎纵向切片 A] ----+
       +----> [引擎纵向切片 B] ----+----> [共享能力网络]
       +----> [引擎纵向切片 C] ----+

纵向切片拥有外部契约和编排权；
共享能力只承载已经确认的一致语义。
```

一个纵向切片从用户入口一直延伸到用户能够观察的结果，包含产生该结果所需的业务判断、状态交接和副作用。项目进度以真实结果衡量；目录、模块、crate 或底层组件的数量不代表进度。

## 2. 从结果出发，自上而下推进

每项任务先明确它要完成的结果，再决定代码结构。开始实现前至少回答：

1. 谁在什么场景下需要这个结果；
2. 成功、失败和必须保持的行为分别是什么；
3. 哪个当前目标对结果负最终责任；
4. 当前目标真正需要哪些直接能力；
5. 本次任务在哪里停止，如何证明已经完成。

内部模块任务以已确认的上游契约和模块自身应交付的行为作为起点；技术组件本身不是目标。

```text
[用户结果 / 已确认的上游契约]
                 |
                 v
             [当前目标]
                 |
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]

========== 当前设计停止线 ==========
```

当前任务只展开目标及其全部直接依赖，到达直接依赖的公开契约就停下来。某个依赖进入后续任务时，把它作为新的当前目标，重新从其上游结果推导职责和直接依赖。

从入口到结果的任务可以逐步扩大实现范围，直到真实结果成立；每次扩大仍遵循同一推导过程，而不是一次性设计整棵依赖树。

## 3. 契约先于实现

先从调用方视角定义承诺，再选择实现形态。一个契约应当说明：

- 调用方提交什么意图；
- 提供方返回什么结果；
- 成功和失败分别意味着什么；
- 哪些复杂性由提供方完整承担；
- 哪些行为是调用方可以长期依赖的。

契约先行不等于 trait 先行，也不等于模块或 crate 先行。函数、类型、模块公开接口或 trait 都可以表达契约；采用满足当前调用关系的最简单形式。确实存在替换、隔离、独立测试或多实现需求时，才引入相应的抽象机制。

接口由调用方的需要决定，以职责语言表达高级意图。算法、存储、协议、第三方类型以及资源限制和调度细节留在实现内部。

跨进程、外部服务或扩展调用同样遵循最小职责信息原则：只提供对方完成当前任务所需的有效事实，内部表结构、持久化身份、调度状态和恢复映射继续留在本方边界。外部协议描述“对方要做什么”；本程序如何实现这件事，是实现者自己的知识。

## 4. 依赖由语义责任决定

项目依赖是能力网络，不是必须逐层经过的阶梯。物理目录、架构图高度和“底层”标签都不决定能否直接依赖。

当前模块可以直接依赖任何自己确实需要的稳定能力，包括位于更底层的核心能力；业务规则、状态和不变量则由真正拥有它们的模块承接。

```text
合理的直接依赖：

       +----> [业务能力 B]
[A] ---+----> [业务能力 C]
       +----> [A 自己需要的核心能力 D]

需要经过语义所有者：

[A] ----> [B：拥有规则与状态] ----> [底层机制 D]
```

判断一条直接依赖是否成立时，依次确认：

1. 当前模块是否对该能力存在独立需求；
2. 调用是否只依赖对方职责级的公开契约；
3. 是否绕过了其他模块拥有的规则、状态或不变量；
4. 候选中间层是否增加了语义转换、策略、保护或生命周期责任；
5. 依赖方向是否保持单向并且能够避免循环。

中间层只改名或转发调用时，直接依赖更准确；中间层拥有不可替代的业务含义时，通过它协作。Composition Root（负责创建具体实现并把它们连接起来的代码）可以直接使用具体实现，以完成构造、配置和生命周期管理；业务执行仍通过相应契约完成。

外部入口和呈现层把业务执行交给相应责任模块，依赖方向始终从入口指向业务，不回头。共享核心能力只提供已经确认的一致语义，不吸收特定调用方的规则；属于单一领域的底层能力可以保留该领域语义，并在当前确实需要时被直接依赖。

## 5. 模块边界与复用演进

模块围绕单一、可命名的职责形成。它负责与该职责相关的知识和变化，把内部状态、算法、第三方细节和复杂流程留在模块内部，对外提供高级接口，让调用方少理解一些。

每次编写具体实现代码前，先检查现有实现能否复用——这项检查贯穿所有开发，不限于新增游戏引擎。新增纵向切片、实现直接依赖、补充根适配器、扩展既有模块或修复共享逻辑时，都先对照现有源码，分别作出以下判断：

1. 语义、规则、不变量、错误语义和生命周期一致的已有能力，直接复用；
2. 只有部分职责一致时，从既有实现中提炼最小、可命名且能独立验证的公共能力，由现有调用方和新调用方共同使用；
3. 仅代码形状相似，但业务语义、约束或演进方向不同时，明确记录差异并保持独立。

新增游戏引擎时，重新检查现有能力的语义、责任，以及当前有哪些调用方在使用它们。语义完全一致的能力，在同一次改动中提炼为唯一的共享实现，并同步迁移全部现有调用方、配置、测试、规格和依赖图；语义不同的能力保持独立。

新增引擎通过真实的结构调整完成：复制现有纵向切片、在共享模块中累积引擎判断分支、增加只负责转发的中间层、保留平行实现，这些捷径都不走。全部受影响代码都改用最终的唯一实现，新增引擎才算完成。

提炼共享能力不要求整个模块完全一致。既有模块中的局部职责只要能够独立成立，并且不携带原调用方特有的规则，就可以提炼为共享能力。提炼时同步迁移现有调用方、测试、现行规格和依赖图，最终只留一套实现。复用的目标是消除重复的知识和机制，既不是强迫不同业务经过同一流程，也不是为了形式上存在一个公共层。

拆分要有明确的工程作用；过度拆分和重复实现都要避免。文件较长、目录整齐、层级对称、代码形式上可以拆开、猜测将来可能复用，都不是新增模块、接口或 crate 的理由。

拆分后的单元只有满足以下任一条件时才成立：

- 具有独立、可命名的职责，有自己的不变量或变化原因，当前确实有调用方需要它，并且能够减少调用方必须理解的内容；
- 形成必要的依赖、编译、发布或所有权隔离。

共同维护同一不变量、总是一起变化，或者离开彼此就无法独立理解和使用的代码，保留在同一模块或 crate 中。好的拆分让读者少跳转：没有只负责转发的中间层，公开接口保持窄小，依赖保持必要且无循环。物理边界始终采用能够完整表达当前语义的最简单形式。

在检查复用和编写实现时持续判断：

- 这段知识由谁负责；
- 它是否形成可以独立命名和测试的职责；
- 当前实现是否混入了不属于本职责的知识；
- 当前调用方，或已确认的后续调用方，是否需要相同语义；
- 提炼后能否减少重复知识，而不只是增加调用层级。

发现可能复用的职责时，先让相关代码具有清晰、窄小的边界。共享接口只随实际需要扩大——实际需要既可以来自现有调用方，也可以来自已经确认、明确需要该能力的后续调用方，不必等到第二份重复代码出现。复用与否取决于语义是否一致；代码形状、函数名称或处理步骤相似，都不算数。

```text
[独立职责]
     |
     v
[切片内清晰边界]
     |  已有调用方需要相同语义
     v
[共享能力模块]
     |  需要独立依赖、编译、测试或发布
     v
[独立 crate]
```

围绕同一职责、共享同一组内部实现并适合共同编译和发布的 `mod` 放在同一个 crate。只有依赖隔离、编译边界、发布边界或所有权边界确实独立时，才提升为新的 crate。

共享能力组成按需连接的网络。每个纵向切片只选择自己需要的能力并保留编排权；共享层服务于切片，而不是演变成统一控制所有业务、要求所有业务经过同一流程的框架。

## 6. 简洁表达、数据类型与配置

代码和数据设计追求“用尽可能少的概念完整表达当前真实语义”。简洁不是机械减少文件、类型或代码行，而是减少调用方必须理解的无效概念。每个类型、字段、状态、接口和中间层都应能够说明由谁负责、当前哪些调用方会使用，以及它消除了什么复杂性。后续需求出现时再逐步扩展现行模型，不为未来预留概念。

进程内部优先使用语言原生、所有权明确并能保证不变量的领域类型。互斥模式使用枚举表达；组合事实使用字段私有的值类型，并通过构造函数建立不变量，使非法状态尽量无法构造。通用文本树、字符串标签和松散键值只用于解析、序列化或持久化；进入内部前，转换成已经校验并能保证不变量的类型。用于诊断的展示文本只做展示，权威标识和解析协议另有主人。

面向人类或模型执行者编写的外部格式也是产品接口。它应当易写、易读、表达力足够，并且适合大规模生成和审核；只保留完成当前意图所需的字段。需要表达复杂内容时，优先采用成熟、广泛理解的既有标准，并且只规定本项目实际读取和处理的语义。

业务模型、输入输出协议、持久化格式和配置不建立版本概念：没有业务版本字段、版本枚举或版本分流，也没有用于兼容、识别或迁移历史格式的旧字段别名或扩展层。元数据和扩展内容只有在当前实现确实读取或处理时才进入契约。

面向 LLM 的 user message 只包含完成本次模型任务所需的最少内容：模型生成当前输出不可缺少、并且尚未由 system message 或既有上下文确定的事实。内部领域模型、持久化结构和设想中的扩展协议留在边界内；能够根据顺序、现有分组或调用方已经校验的状态稳定推导出的内容，不再重复编码。

默认沿用最短、直接、易读的现有表达。只有当前任务确实需要无歧义的嵌套或转义，并且现有表达无法支持时，才改成 JSON。包装层、标签、空字段、重复计数和重复约束只在承载真实语义时存在；统一格式、方便机器读取、便于调试、设想将来扩展，都不是增加它们的理由。

任何新增内容都必须说明模型当前如何使用它，以及为什么缺少它就无法正确完成任务。

外部松散字段只有在当前流程确实需要读取、写回或原样转发时才保留，并且只停留在相应边界。原样转发本身可以构成实际需要；业务模块依然不需要理解这些不透明字段，内部模型也不因为“将来可能有调用方使用”而扩大。

项目实行严格的单版本实现原则。现行规格是系统唯一有效的版本定义，每次版本迭代都以新规范一次性替换旧规范。迭代完成后，生产代码、测试、测试夹具、配置、现行规格、代码注释、错误类型、诊断、日志和用户提示只描述并实现当前版本。

旧版本随迭代整体退出：兼容、识别、检测、探测、专用拒绝、迁移、转换、回退、降级、警告、提示、条件分支、旧格式解析器、旧字段或旧值别名、双读双写，以及仅为证明旧版本行为而存在的测试和样本，一律不留。

所有输入只依据当前规范解析和校验。不符合当前规范的内容统一作为普通无效输入处理，使用当前契约已有的通用错误语义。系统不判断、不记录、不提示该输入可能属于旧版本，也不通过版本号、特征字段、格式指纹或失败后的备用解析路径推断其历史身份——旧版本数据和调用方不构成当前实现的运行时场景。

版本切换在同一次改动中删除旧实现及其全部残留，并同步更新当前调用方、数据定义、测试、配置、现行规格和依赖图；不设过渡期兼容层，旧实现一次清完。

已有数据若不符合新规范，在当前实现之外完成替换、重建或清理；迁移器和专用识别逻辑不进入当前代码库。工具链要求的包版本或构建版本只标识当前构建结果，与业务分流、输入识别和历史版本枚举无关。

引入资源限制和调度机制前，先证明它们确有必要。外部服务配额、操作系统或协议的硬性限制、已经测量的性能瓶颈、能够复现并会导致故障的资源竞争——只有这些证据能支持引入并发上限、队列、超时、批量、重试、等待窗口或容量拒绝。

“配置已经存在”“已有代码读取该配置”“参数可以调节”“这项机制看似能保护用户电脑”，都不是必要性证据。先有证据，后有机制；顺序不反过来让配置为机制找数值。

证明机制必要后，再根据谁掌握相应事实来决定参数取值。部署环境、外部服务或操作者确实能够选择的策略进入统一配置；固定的领域事实和产品语义由代码契约规定；并行工作单元数量、调度窗口、传输批次、重排窗口、连接复用和缓存策略，由执行代码根据基准测试和运行时数据决定。

影响资源占用不等于必须交给用户配置，内部实现细节不伪装成用户的业务选择。缺少必填的外部选择时明确失败；没有实际选项的模块保持无配置对象。

项目规模不是可配置策略，ATT 也不自行规定产品上限。文件字节数、目录项数、目录深度、目录总字节、Lua 值大小或复杂度、SQLite 查询组数、行数或结果字节，以及 Claim、Unit、Group、Task 等业务总量，都没有人为上限、拒绝分支或对应配置。

“无上限”意味着这项比较和拒绝逻辑根本不存在，而不是把默认值换成一个很大的常量。输入只会因真实的文件系统、操作系统、地址空间、内存、SQLite、外部协议或数据格式错误而失败。程序不替操作者预防资源耗尽而提前拒绝规范内的输入，并且始终完整报告实际失败原因。

命名、类型和注释共同表达领域责任。命名优先回答“这是什么职责或事实”。注释重点解释业务含义、不变量、来源映射，以及仅从代码本身看不出的取舍；代码表面动作留给代码自己说，需要保留的原始事实用原名，臆造的语义名称不盖在它们上面。

## 7. 语义所有权、信任与错误

每项业务事实、规则和不变量都有唯一责任方。最了解外部输入含义的边界负责解析和校验，并把输入转换成能够保证不变量的内部类型。转换成功后，内部模块按契约直接协作，同一事实只校验一次、只解释一次。

```text
[外部事实]
     |
     v
[责任边界：解析、校验并转换为内部类型]
     |
     v
[能够保证不变量的内部数据]
     |
     +----> [内部模块] ----> [内部模块]
```

内部信任是对契约和已建立不变量的信任，同时正视实际可能发生的失败。静态业务语义在所属边界建立一次；会随时间变化的外部状态，由真正执行副作用的模块在操作发生前重新观测。输入错误、运行环境失败和内部不变量破坏保持不同含义。

错误由责任边界转换成调用方能够理解和处理的形式，同时保留定位问题所需的完整原因和上下文。产生具体错误的代码仍然掌握类型、阶段、路径、字段、数值、稳定错误码和提交最终状态时，就构造一份已经按敏感信息规则处理的结构化错误记录。

CLI、日志和其他输出只读取这份记录；顶层不做 downcast（向下类型转换），不解析 `Display` 文本，也不遍历任意错误对象来猜测事实。

错误和日志明确说明具体失败内容，以及已经按敏感信息规则处理的底层原因。只要相应事实存在，输出就展示命令和阶段、路径或对象、实际值和期望值、OS/SQLite/HTTP 等稳定代码、底层原因、状态是否改变、处理办法和恢复位置。

责任域分类只作补充；“输入错误”“项目不可用”“运行失败”“内部错误”这类泛化类别不能替代具体诊断。主错误、相关错误和清理错误各自完整呈现，谁也不覆盖谁，队列压力也不是丢失它们的理由。

敏感信息只由现行规格通过封闭清单明确列出；模块、日志、诊断、文档和测试都不按内容类别、用途或主观判断自行增加项目。清单成员、精确替换方式和内容边界只由 [Chat Completions 运行根规格](docs/runtime/chat-completions.md#6-敏感信息闭集唯一权威)规定。

其他文档和代码只引用该规定，不复述清单，也不派生自己的清单。隐藏清单中的值时，它所在的字段、正文、阶段、路径、字段名、状态码、SQLite 或 OS 代码、实际值与上限、事务和发布最终状态都完整保留；保护敏感信息不是省略现行规格允许呈现详情的理由。

业务允许的未产出、部分结果和暂时不可用属于正常结果，结果不完整本身不是程序错误；只有模块已经无法继续履行契约或保持状态一致时才返回错误。正常结果仍包含足够的结构化诊断信息，并由统一负责日志和指标的模块记录，业务模块不各自拼接和分散保存日志。

日志、指标和追踪等可观测性记录默认只是业务行为的附属记录，不是权威业务状态。业务逻辑不根据这些记录判断状态、决定提交、执行恢复或重放，也不用它们决定是否允许产生副作用。普通的记录操作失败时，产生已经按敏感信息规则处理且用户可见的诊断，原业务结果、项目状态和退出码保持原样。

日志、任务记录、进度和其他呈现能力一旦确认失败，负责该能力的边界立即建立结构化诊断；不能等到长时间命令结束后才首次告知，也不能只在内存里累计一个最终可能无法送达的横幅。项目日志仍可写时，Partial、Unavailable、跳过、人工处理、取消后已生效状态和旁路记录失败等事实进入当前 RunId 的 JSONL；项目日志自身无法建立或继续写入时，终端及时显示包含阶段、对象、稳定代码、原因和处理办法的警告。可选的 Markdown 或调试文件不作为这些诊断的唯一载体，关闭它们不能让原因消失。

事件名、事件正文和字段含义必须与已经确认的事实一致。只有阶段实际完成时才写完成事件；统计事件使用不表示提交或成功的中性措辞；调用次数包含已经进入 Host 的失败调用，改动行数只记录实际执行返回的行数。事务是否提交、失败、取消或结果未知只由相应终态事件表达，普通统计和进度事件不得提前或反向宣称成功。

记录失败本身不改变业务结果、项目状态和业务退出语义；但向使用者呈现该警告的 stdout/stderr 写入、flush、后台线程或 channel 失败属于独立的进程呈现失败，必须返回失败退出码。任何用户可见警告、错误、进度终态或相关诊断都不能用 `let _ =`、空错误分支或无诊断的静默退化吞掉。日志队列允许按现行规格丢弃普通事件时，最终诊断至少报告实际丢失数量、日志对象和发生阶段。

只有已经确认的用户结果、产品契约或外部约束明确规定“证据必须完整，操作才允许开始、成功或保持一致”时，相关记录才属于业务状态或具有权威性的审计记录。此时，现行规格必须明确哪些组件会读取这些记录、这些记录是否权威、如何持久化，以及记录失败意味着什么。记录内容重要、敏感、需要完整或需要长期保存，本身都不能证明它应成为业务操作的前置条件。

开放扩展能力时，先明确哪些输入可以信任，以及谁负责相应协议。扩展拥有自己的数据协议、生命周期、事务和跨阶段交接时，核心只向它提供约定的宿主能力和已经校验的上下文；扩展的不透明产物由扩展自己掌管。只有扩展明确采用核心契约时，双方才共享相应语义。统一产品入口尊重扩展的表达能力，不强迫所有数据进入同一标准流程。

可编程扩展、插件、脚本、自定义适配器和其他调用方需要把结果交给核心管理时，如果现有核心契约能够完整表达相同的语义、规则和不变量，调用方应把自己的私有表示转换成该契约接收的内部数据，由语义所有者在任何受管状态改变前统一完成验收、冲突处理和提交。这些规则只有一份：调用方不复制它们，不绕过契约直接修改权威状态，也不在后续阶段临时改写结果而形成多个权威来源。

现有核心契约无法表达调用方的真实语义时，调用方可以使用低级能力或保留私有协议，并完整承担相应的解析、校验、状态、一致性、事务、恢复、幂等和往返验证责任。核心可以提供可选的低级公共能力；没有经过核心契约验收的结果保持其私有身份，不被宣称为核心已经保证的受管状态。公共契约的作用是提供更安全的选择，扩展表达能力始终受尊重；多个真实调用方需要相同语义时，再按现有复用规则提炼最小公共能力并迁移相关调用方。

## 8. 状态与持久化来自真实交接

状态模型从真实纵向行为之间的交接产生。只有后续行为确实需要跨时间、跨进程或跨阶段保留的事实才进入持久化。

领域上下文的范围、耗时工作的分配单位、结果验收单位和持久化原子单位分别由各自的不变量决定，彼此可以不同。相关事实可以作为整体提供上下文；能够独立确认的结果按最小有效单位验收；必须共同保持一致的传播或状态关系，在同一个原子事务中提交。批量工作里每项有自己的成败。已经确认且保持一致的进展及时保存；未完成部分由后续运行根据权威状态重新推导。

批处理和流水线需要证据才存在：真实的领域关系、外部协议容量、已经测量的固定成本或吞吐瓶颈。必须作为整体处理的语义单元保持完整，相关上下文尽量保留。已经证实的固定成本可以分摊到更多工作上；无关内容不为了“装满批次”而拼接，相关内容不无缘无故切碎。

项目总工作量与同时处理的工作量严格区分。Claim、Unit、Group、Task、文档和 SQLite 行的总量不受内部窗口限制。流水线窗口只限制正在读取、计算、等待提交或执行外部请求的工作；窗口已满时，后续工作排队等待，而不是失败。

内存可以用于索引、缓存、重排和保存已经完成的结果，以提高吞吐量。磁盘暂存队列、逐项消息往返和整条执行路径串行化，只在有换页、内存耗尽或代表性负载问题的证据时引入。并发可以改变完成时间；自然顺序、代表选择、分组结果和提交顺序保持原样。

```text
[当前行为]
     |
     | 后续行为必须继续使用的事实
     v
[持久化交接]
     |
     v
[后续行为]
```

每项持久化事实都应能够回答：

1. 谁负责写入；
2. 谁负责读取；
3. 它支持什么可观察行为；
4. 哪个位置是权威来源；
5. 它何时创建、更新和失效。

能够从权威事实稳定推导的数据在使用时计算。共享存储可以承载其他领域提供的信息，解释权仍属于该领域。数据结构随已经确认的纵向切片小步演进，每个字段的存在都由真实读写关系证明。

持久化结构优先直接表达领域资产及其真实读取、更新和事务边界。共同构成语义整体并经常一起读取的事实保持明确关联；需要独立继承、校验和写回的叶子保留精确身份。通用中间模型只在出现真实的跨领域统一读写需求时引入——清晰的领域关系，不换成更通用但表达力更弱的结构。

### 8.1 文本提取层次、任务装箱顺序与幂等性

`Unit`、`Group` 和 `Semantic Scope` 首先是文本提取完成后的统一整理结构；它们与 `TaskBlock`、`Task ID` 共同构成所有翻译引擎遵守的任务规划契约。各层只承担以下职责：

- `Unit` 是能够独立判断是否需要模型处理、独立验收并独立提交或传播译文的最小叶子，其身份和自然顺序来自数据源。必须共同生成和验收的多行或多项内容属于同一个 `Unit`，不按显示行拆成多个 `Unit`。
- `Group` 是必须完整提供给模型的最小语境，由自然顺序确定的一组 `Unit` 构成。一个 `Group` 不跨 `TaskBlock` 拆分；其中某些 `Unit` 不需要模型处理时，仍保留在原位置参与语境。单个 `Group` 超过装箱目标时，保持完整并单独形成 `TaskBlock`。
- `Semantic Scope` 是允许若干有序 `Group` 共同装入 `TaskBlock` 的最大领域范围。它只来自数据源本身的结构关系；`TaskBlock` 不跨 `Semantic Scope`，也不改变其中 `Group` 的自然顺序。数据库文件、地图、公共事件、插件或 Generic 文件如何映射成该范围，由相应引擎的提取契约规定。
- `TaskBlock` 是在一个 `Semantic Scope` 内按完整且连续的 `Group`、自然顺序和装箱目标确定的稳定语境容器，包含所装 `Group` 的全部 `Unit`。它表达哪些内容应当一起提供给模型，不表达本轮还有多少内容需要翻译，也不是根据待译项临时拼出的工作批次。
- `Task ID` 是完整 `TaskBlock` 确定后，只分配给本轮模型代表的块内临时编号。它只用于关联本次请求与响应，不是 `Unit`、`Group` 或 `TaskBlock` 的持久身份，每个实际发送的 `TaskBlock` 都从 `1` 开始连续编号。

各引擎的 Extract 责任边界必须把原始数据中的文本叶子整理成上述前三层，再交给 Planner：每个提取出的文本叶子拥有确定的 `Unit` 身份和自然顺序，每个 `Unit` 恰好属于一个完整 `Group`，每个 `Group` 恰好属于一个 `Semantic Scope`，同一范围内的 `Group` 具有确定的自然顺序。`Unit` 的角色、`Group` 的完整语境和 `Semantic Scope` 的范围都根据数据源本身的含义与结构建立，不由翻译状态、去重结果、装箱 Profile 或任务大小决定。

物理文件、资产 owner、读取批次、规则来源和适配器边界本身不构成新的语义边界。多个提取来源共同描述同一领域范围时，Extract 的整理结果按数据源语义把它们归入正确的 `Group` 和 `Semantic Scope`，并建立统一自然顺序。Planner 直接使用这份完整层次，不重新扫描原始路径来猜测关系，不按当前待译项重新分组，也不因为输入来自不同提取实现而切断本应共同提供的语境。

任务规划的语义顺序固定如下：

```text
[完整数据源快照]
        |
        v
[Extract：Unit 与完整 Group]
        |
        v
[Extract：Semantic Scope 与自然顺序]
        |
        | 不读取译文状态、模型责任或临时 ID
        v
[完整、稳定的 TaskBlock]
        |
        v
[判断 Current、源语言、保护内容、复用与全局去重代表]
        |
        v
[只给模型代表分配 Task ID；其余 Unit 保留为无编号语境]
        |
        v
[过滤零 Task ID 的 TaskBlock]
        |
        v
[模型执行流水线]
```

装箱阶段使用完整数据源中的全部 `Group` 和 `Unit`。它不读取或依赖译文是否存在、译文是否 Current、源语言判断、保护结果、复用结果、全局去重代表、临时编号、模型响应、任务记录或重试历史。只有完整 `TaskBlock` 的成员和边界确定后，才判断模型责任并分配 `Task ID`。

实现可以为了并行或减少重复计算而提前算出状态候选，但这些结果不能成为装箱输入，也不能改变完整 `TaskBlock`。阶段名称描述语义依赖，不要求把彼此独立的计算机械串行执行。

只有本轮模型代表获得 `Task ID`。已有 Current 译文、相同原文在其他 `TaskBlock` 中已经选出模型代表、源语言判断为无需翻译、内容完全受保护、能够直接复用译文，以及其他不需要模型产出的 `Unit`，都不获得 `Task ID`，但仍按原顺序留在所属 `Group` 和 `TaskBlock` 中作为语境。有有效目标文本时，无编号语境呈现目标文本；否则呈现经过当前保护规则处理的源文本。

分配完 `Task ID` 后，整个 `TaskBlock` 没有任何编号项时，只把该块从本轮模型执行流水线中滤除。这个过滤不删除或改变权威的完整装箱结果，不把过滤后相邻的块重新合并，也不改变其他 `TaskBlock` 的成员和边界。一次 Partial 后再次规划时，已经接受的 `Unit` 变成无编号语境，未接受的模型代表获得新的块内临时编号；只要完整装箱输入没有变化，它们仍属于原来的 `TaskBlock`。

在数据源快照、提取产生的 `Unit`、`Group` 与 `Semantic Scope`、自然顺序、装箱 Profile 及其他明确参与装箱的稳定输入相同时，完整装箱结果必须幂等：`TaskBlock` 的数量、顺序、边界以及每个块包含的 `Group` 和 `Unit` 完全相同。译文状态和模型责任变化只能改变哪些 `Unit` 获得 `Task ID`、无编号语境呈现原文还是译文，以及哪些零编号块不进入执行流水线，不能改变完整装箱结果。字符数装箱使用由完整源数据派生的稳定表示，不得使用会随这些运行状态变化的译文长度、编号有无或条件渲染内容，否则不满足该幂等契约。

这里要求的是完整装箱结果的结构幂等，不要求持久化 `TaskBlock`，也不要求不同翻译状态下最终发送的消息逐字相同。Planner 可以每次从权威数据源重新计算，只要相同装箱输入必然得到相同结构。

这套提取层次和规划顺序属于跨引擎共享产品语义，由唯一的共享规划契约负责。MV、MZ、Generic 以及后续新增引擎可以各自定义原始数据如何映射为 `Unit`、`Group` 和 `Semantic Scope`，以及不同 `Unit` 的消息呈现形状；不得各自改写三层含义，也不得改写装箱、模型责任、临时编号和零编号过滤的先后关系。语义一致的 Planner 能力使用同一共享实现；引擎适配器只提供数据源映射和消息呈现，并共同通过一组契约测试，不能以测试结果暂时相同为由长期保留多套重复 Planner。

维护 Planner 或新增引擎时，契约测试至少用同一个完整数据源分别覆盖全部待译、部分 Current、跨块去重、无需源语言翻译、完全保护和全部 Current 等状态，并断言完整 `TaskBlock` 清单始终相同；测试另外断言只有 `Task ID`、无编号语境的呈现内容和实际执行块集合随状态变化，零编号块过滤后没有重新装箱。

## 9. 性能、并发与资源限制

性能是核心产品契约和完成条件，与正确性并肩，不是事后可选的优化。选择 Rust 的价值必须体现为：大型项目上的实际等待时间（wall-clock time）更短、能够有效使用多个 CPU 核心、减少重复工作，并提供可预测的原生执行。“使用了 Rust”“使用了异步或 Rayon”“增加了线程”都只是手段，实测结果才算数。

### 9.1 何时验证性能

是否需要验证性能，取决于改动是否影响核心执行路径或某项已经说明作用的性能机制，与改动行数、文件数量、提交规模无关。

以下改动不运行性能基准：不改变核心执行路径的文档和本地化改动、性能基准不读取的普通测试夹具，以及不在常用执行路径上的诊断代码改动。性能测试夹具、测量脚本或代表数据发生变化时，先重新确认测量流程和既有基线仍然适用。

改动涉及以下内容时，先通过静态复杂度分析或只测量受影响部分来判断风险：

- 算法复杂度或数据流；
- 并发和同步；
- 存储访问和文件扫描；
- 常用执行路径上的日志或序列化；
- 外部调用。

只有这些改动可能产生可测量的影响，或者已经观察到性能差异时，才与固定的基线进行正式比较。探索中的实现先验证受影响的热点；准备保留的最终实现才在真实游戏上完成验收。

没有证据时，保持现状：已有并行处理保留，不增加串行等待点、重复扫描或解析、磁盘中转、逐项事务、逐项立即落盘，也不引入队头阻塞。

### 9.2 测量环境

需要性能验证时，MV/MZ 使用测试集中规模最大且结构最复杂的真实游戏，Generic 使用大型
JSONL 样本。只影响一个引擎时测对应主样本；影响公共核心路径时两类主样本都要测。在被
Git 忽略的 `tmp/` 下建立独立的测试工作区，并保存测量脚本、原始记录和报告。

测量使用通过 `Release` 配置和 MSVC 工具链构建的实际可执行文件，每次完整执行都准备全新的来源副本、项目工作区、数据库和输出目录。前一轮的 `Extract`、译文或 `WriteBack` 结果不复用；以下缩短时间的方式都不可用：

- 使用 `unchanged` 快速路径；
- 预热应用自身的缓存；
- 减少提取覆盖；
- 跳过校验；
- 降低持久化或发布保证。

操作系统缓存必须受到控制，并在报告中说明处理方式。现行语义要求在单次运行内执行的全局去重，属于正常工作，不算利用缓存缩短测量时间。

### 9.3 比较轮次与停止条件

使用受影响领域中最大且最复杂的真实样本选择最终实现。公共能力同时使用最大的真实
RPG Maker 样本与大型 Generic JSONL 样本。其他代表样本各做一次成对回归，只用于发现
明显退化或样本特有错误；不同游戏、两类引擎样本各有各的分布，不混作同一统计总体。

正式比较先运行两轮成对交错测试，第二轮交换两个实现的先后顺序。两轮方向一致，并且结果已经明确达到接受或拒绝条件，立即停止。结果冲突、接近判断条件，或者存在能够说明的环境异常时，才运行第三轮。第三轮后仍无法判断时，保留现行实现并记录证据不足，轮次到此为止。

开始测试前，根据预检的实际耗时说明：

- 剩余测试预计需要的总时间；
- 最多运行多少轮；
- 哪些结果会让测试提前停止；
- 整个测试允许使用的总时间。

达到接受或拒绝条件、发现正确性差异，或者下一轮会超出时间预算时，立即停止。需要增加预算时，先说明额外时间能够改变什么决策，并取得使用者明确同意。

常规的两至三轮测试只报告每轮原始值、成对差值、中位数和范围；p90、p95 等尾部分位数需要更多样本，这里不声称。详细流程只由 [性能验证指南](docs/performance-validation.md) 规定。

### 9.4 性能目标

在固定的基准机器和上述最大真实游戏上，三项核心路径的目标分别是：

- `Extract`：尽量把总耗时压缩到 10 秒以内；
- `Translate`：排除模型供应商实际生成内容和网络等待的时间后，把项目读取、预处理、全局去重、任务构造、响应验收、传播和持久化的总耗时压缩到 10 秒以内；
- `WriteBack`：把完整资产读取、引擎改写、候选构建、验证和单次发布的总耗时压缩到
  10 秒以内。

测量 `Translate` 中 ATT 自身耗时时，使用能够立即返回确定且合法结果的本地 `Provider`，所有真实任务和提交工作照常完成。任何一个阶段的耗时还未降到 10 秒以内时，性能工作就未完成；保留各阶段的测量结果、瓶颈证据和下一步仍需优化的内容。

### 9.5 正确性条件

性能只从真实优化中来；逻辑错误、减少行为和降低核心能力都不是代价选项。以下行为都是不可放宽的验收条件：

- 提取覆盖和自然顺序；
- 指纹和全局去重；
- 代表项与 `seed` 选择；
- MV/MZ Mutation Claim 冲突与 Generic 动态输入一致性；
- 完整语料形成的稳定装箱边界、完整语境，以及装箱后分配的块内临时编号；
- 事务原子性和部分翻译进度；
- MV/MZ Rules、Generic JSONL 同步，以及改动原子数据库 Lua 时的事务语义；
- 取消和候选清理；
- 完整验证和单次目录发布。

输出或状态存在任何逻辑差异时，性能成绩无效。发现核心路径中存在超线性算法、重复工作、错误状态或不一致行为时，修复后再交付，它们不作为已知问题保留。

### 9.6 并发方式与参数

根据工作负载选择并发方式。等待型 I/O 使用能够在窗口已满时让上游等待的异步处理，或使用专门执行阻塞操作的资源；CPU 密集型工作使用受控的数据并行。

独立文档、Group、规则、任务和响应默认并行处理，或与相邻阶段重叠。串行只留给真正需要它的地方：明确的数据依赖、必须按确定顺序合并结果、单写者事务、单个 VM 或最终发布边界。串行范围尽量短，并说明无法并行的具体原因；“保持顺序”本身不足以让全部计算串行执行。

异步和多线程设计明确哪些工作会同时运行、窗口已满时如何等待、如何取消、错误如何传递，以及每个任务何时结束。内部并行工作单元数量、窗口大小和批次大小按以下方式选择：

1. 在最大真实样本上，用限定数量的对照实验比较不同参数；
2. 通过预检排除明显较差的参数；
3. 只让最有希望的参数进入两轮成对比较；
4. 选择吞吐量已经接近最高、继续增加资源也几乎没有收益的一组参数。

参数与样本的完整组合不全跑。内部参数由执行代码根据基准测试或运行时数据决定；供应商并发限制、RPM 和网络超时等真实外部约束才进入配置。并发保证结果确定性、持久化一致性和错误语义不变。

### 9.7 优化与验证顺序

性能优化先用分阶段计时、算法复杂度分析和必要的性能分析器（profiler）证明瓶颈，再针对已经证实的瓶颈实施。正确性验证随候选实现的成熟程度逐步扩大：

1. 固定基线的构建结果、源码、配置和既有验证记录都没有改变时，复用已经记录的完整验证；没有适用记录时，只补做一次；
2. 探索中的实现开始计时前，只运行受影响的专项测试、相关契约测试和结果等价检查；因静态检查或性能不足而淘汰后，不补跑全部验证；
3. 准备保留的最终实现运行一次完整工作区测试、完整契约矩阵和失败路径验证；
4. 候选实现改变了相应的外部契约或关键语义时，在计时前先扩大受影响的验证范围。

每轮计时都重复执行真实受影响路径，并核对退出码、结果哈希和必要的最终状态。任何结果不等价都会使该轮性能成绩失效。

CPU、磁盘、同步、事务和峰值内存等详细数据，只在定位瓶颈或验证相应资源假设时单独采集；不要求每个候选、游戏和轮次都采集。展示结果用真实的成对数据；单次最好成绩、缓存命中的结果和无法量化收益的复杂设计都不作数。核心路径相对固定基线出现严重性能下降时，不交付。

某项性能下降确实无法避免时，提供分阶段测量和性能分析记录，并说明：

- 新增成本对应什么必要语义或外部硬性限制；
- 已经尝试过哪些优化；
- 为什么在不破坏正确性和产品契约的前提下无法继续降低成本。

“实现更简单”“改动更统一”和未经验证的推测都不是解释。真实测试集和 `tmp/` 中不纳入版本控制的测量脚本，可以根据当前风险直接调整，旧测量材料不需要兼容处理。

## 10. 实现、验证与完成定义

当前版本的用户可观察契约、持久数据定义和现有外部调用方，决定本次改动必须同步更新哪些内容；旧版本不带来兼容责任。版本迭代一经确认，旧契约、旧数据格式和旧调用方就退出当前范围，兼容、识别、拒绝、迁移和提示逻辑随它们一起退场。

既有结构无法准确表达当前语义、产生重复知识或持续阻碍实现时，进行范围明确的破坏性重构：在同一次改动中更新全部当前调用方、测试、配置、现行规格和依赖图，彻底删除旧实现。转接层、新旧两套模型、只处理历史版本的错误类型，都不是保存失效设计的方式。

继续实现直接依赖时，除根接口外，所有包含领域判断、状态转换或编排规则的能力都应获得真实实现。最终的根接口只负责文件、计算、存储、网络、时间和运行时等环境机制。Composition Root 统一负责转换外部配置、创建具体实现并管理其生命周期。

以下内容代表不同的完成程度，交付时分别说明：非根业务算法已经成立、根接口已经定义、根适配器真实可用，以及生产入口已经连接这些实现并能完整执行。

实现顺序与设计顺序保持一致：

1. 用高层行为固定当前目标的可观察结果；
2. 根据调用方的需要定义直接依赖的最小契约；
3. 实现并验证当前目标，范围外依赖使用测试替身；
4. 某个依赖进入实现范围后，再以其上游契约为起点实现并验证；
5. 任务要求从入口运行到结果时，用最小真实样本完整执行这条路径；
6. 共享能力有多个调用方后，验证各方依赖的是同一语义。

依赖暂未实现时，可以使用测试替身验证当前目标。生产入口只连接已经具备真实实现并完成相应验证的能力。契约完成、局部测试通过、底层组件就绪，各自都不能单独证明纵向切片已经完成。

进程级主成功测试和端到端测试使用当前发行配置的正式默认行为。Thinking、任务记录、项目日志、完整校验等默认开启的能力不得为了缩短测试或简化公共夹具而关闭；显式关闭只在单独的选择测试中出现，并同时保留默认开启的主路径。网络代理的 `false` 表示直连，不属于功能关闭，按网络拓扑分别测试。

新增或修改配置时，同一次改动必须验证字段定义、解析、校验、实际应用和生产入口，并分别覆盖省略后的正式默认、显式开启、显式关闭和无效值。配置控制文件或日志产物时，端到端测试直接检查目录是否建立、文件数量、RunId 关联、必要内容、零工作量行为，以及建立、写入、flush、关闭或呈现失败时的用户警告和退出语义。组件测试自行预建目录、保持执行器存活或绕开真实关闭顺序，不能替代生产生命周期测试。

错误与可观测性测试使用可控制的失败注入覆盖日志启动、队列饱和、序列化、写入、flush、sync、worker panic、任务记录和 stdout/stderr 呈现失败。测试断言具体诊断、持久日志、状态影响和退出码，不能只断言命令“没有 panic”或汇总数量；还要断言必要事件恰好出现一次、计数与实际调用一致，并且失败路径没有完成、提交或成功文案。重构进程测试时，原有的默认开启、失败可见性、取消和终态测试必须迁移到新结构；删掉调用入口或公共夹具不得同时删掉这些行为证据。

当前任务完成，意味着约定范围内的结果或上游承诺已经成立，失败语义明确，并且已经完成相应验证。完整纵向切片完成还要求：用户结果能够通过实际生产入口获得，必要状态能够正确交接，相关副作用和资源边界已经验证。代码、文档和交付说明明确标出尚未实现、尚未连接或尚未验证的范围。

架构图、现行规格和测试随已经实现并验证的事实同步更新。图只描述真实依赖或明确标注的候选设计，设想不写成现状。

文档中描述的已实现行为、职责边界、调用关系、入口、约束、示例和路径，都能由当前源码和测试验证。经确认的当前契约或实现发生变化并使这些说明失效时，在同一次改动中修正文档，并使用与源码一致的职责名称和含义。源码、测试和现行规格发生冲突时，先确认当前有效契约，再修正不符合契约的一方；规格不为了迁就现有代码而改写。尚未实现的内容明确标为设计目标，与现状分开写，维护者不需要通过比对源码猜测哪一处说明已经过期。

开始实现前，先从预期的外部结果反查源码、测试、现行规格、指南、Skill 和发行物，将它们纳入影响检查范围，并把确认受影响的内容纳入实施范围。任务说明没有逐项点名某类材料，不构成不检查或不更新的理由。

从使用者或执行者的外部视角能够观察到的功能、行为或流程经确认发生变化时，在同一次改动中更新负责相应语义的全部内容：

- 能力范围、输入输出、权限、副作用、环境要求、成功失败或恢复语义变化时，同步更新现行规格和测试；
- 调查事实、选择能力、组织操作或验证结果的方法变化时，同步更新指南；
- 任何外部功能或流程变化都必须检查覆盖该场景的现行 Skill；变化会影响执行者的适用范围、前置条件、必读材料、状态选择、操作顺序、授权与副作用、完成证据、停止条件或失败恢复时，同步更新 Skill 包中负责相应流程的内容；
- 发行内容或使用入口变化时，同步更新发行资源、导航和发行验证。

执行者的阅读、选择、授权、操作、验收或恢复方式已经变化时，Skill 自身同步更新，只改它引用的现行文档不够。现行 Skill 已经完整覆盖变化，并且上述执行判断均未改变时，不复制产品事实、不制造文件差异，但必须在交付中说明影响检查的结论。仅有内部实现变化且外部行为没有变化时，Skill 保持原样。

源码与测试、现行规格与指南、受影响的 Skill 和发行物共同描述同一个当前版本。这里的一致是事实、名称、行为和流程一致，同一内容不需要逐字复制到多个位置。任何一处仍描述旧行为、遗漏当前流程、存在已知冲突、失效链接或过期导航时，任务尚未完成。只有长期产品方向、架构判断方法、项目边界，或者知识归属和同步规则发生变化时，才更新本文件。

## 11. 每次任务的方向检查

设计、实现和评审都用以下问题检查方向：

1. 当前工作最终服务什么用户结果或上游承诺；
2. 谁拥有该结果及其中的规则和不变量；
3. 当前目标的直接依赖分别是什么，为什么是直接依赖；
4. 是否存在只负责转发、没有增加规则或保护的中间层，或者绕过语义所有者的调用；
5. 新模块抽取的是稳定职责还是未经证明的未来设想；
6. 编写具体实现前是否已经检查现有完整能力和局部职责，并说明哪些直接复用、哪些提炼共享、哪些因语义不同而保持独立；
7. 候选拆分是否具有独立职责和实际收益，还是只让文件更短、目录更整齐或层级更对称；拆分后是否反而产生转发层、迫使读者在更多文件间跳转、扩大公开接口或增加不必要的依赖；
8. 外部事实在哪个边界完成校验并转换成能够保证不变量的内部数据；可编程扩展把结果交给核心管理时，是否复用了语义所有者的现有契约；无法复用时，是否明确承担了私有协议的完整责任，并避免把未经核心验收的结果当作核心受管状态；
9. 新增状态是否有明确的写入方、读取方和行为用途；
10. 哪些证据能够证明纵向结果，而不只是证明局部代码可运行；
11. 每个并发上限、队列、超时、批量、重试、等待窗口、容量拒绝或磁盘暂存队列，分别由什么外部硬性限制、可复现故障或测量证据证明；是否错误地把“已有配置”“已有代码读取”或“可以保护用户电脑”当成必要性证据；
12. 内部模型是否用强类型直接表达不变量，外部格式是否保持最小且易于大规模书写；
13. 已经证明必要的策略，为什么分别由外部配置、代码契约或执行代码根据运行时数据决定；是否错误地把并行工作单元数量、队列、批次和缓存交给用户配置；
14. 是否对项目总量建立了任何 ATT 自行规定的文件、目录、Lua、SQLite、Claim、Unit、Group 或 Task 上限；所谓无上限是否真正删除了比较和拒绝路径；
15. 各输出是否明确展示已经按敏感信息规则处理的底层原因、阶段、路径、稳定错误码和最终状态；是否用泛化类别或保护敏感信息为由省略了允许公开的详情；
16. 新增的日志、指标或追踪记录是否仍然不参与业务判断；如果记录失败会改变业务结果或项目状态，哪些现有组件会读取它，以及哪项已确认契约证明它属于业务状态或权威审计记录；
17. 批处理、流水线、验收和提交单位是否分别符合领域关系、实测成本与一致性要求；处理窗口已满时是否只让后续工作等待，而不限制总工作量；
18. 改动是否影响核心执行路径，并能指出可能产生可测影响的算法、并发、存储、序列化、
文件、同步或外部调用机制；若有，是否先只测试受影响部分来筛选候选，并且只让准备保留
的最终构建结果按影响范围使用测试集中最大、最复杂的真实 RPG Maker 游戏与大型 Generic
JSONL 样本，在 `tmp/` 的全新状态下完成没有缓存捷径的 `Release` 验收，持续逼近或达到
`Extract`、`Translate`、`WriteBack` 各自 10 秒的目标；
19. 性能验证是否在开始前说明总时间预算、最多轮数和提前停止条件；是否错误地把不同游戏当成同一分布的样本计算分位数，或者在证据已经足以接受、拒绝或保持现状后仍继续增加轮次；
20. 性能结果是否保持全部核心语义且没有严重下降；无法避免的性能下降是否有测量、性能分析、必要语义和已尝试优化的完整证据，而不是通过减少工作、降低保证或主观解释来掩盖；
21. 新增领域事实是否已经由真实样本、权威说明或当前调用方的实际使用验证；
22. 版本迭代后是否已经从生产代码、测试、夹具、配置、现行规格、注释、错误和诊断中删除旧版本的全部痕迹，并确保不符合当前规范的输入只按普通无效输入处理；
23. 源码与测试、现行规格与指南、受影响的 Skill 和发行物是否描述同一当前功能与流程；外部功能或流程变化是否已经检查覆盖该场景的 Skill，受影响的 Skill 是否在同一次改动中更新，无需修改的结论是否有明确依据；Skill 是否仍只组织执行并按需读取权威事实，没有复制契约或绑定特定执行平台。
24. 各引擎是否在 Extract 后按共同定义整理出完整的 `Unit`、`Group` 和 `Semantic Scope`，并由翻译 Planner 先从这份完整结构幂等地建立 `TaskBlock`，再判断模型责任和分配 `Task ID`；无编号 `Unit` 是否仍完整保留语境，零编号块是否只在执行前过滤且没有触发重新装箱；所有现有和新增引擎是否通过同一规划契约及跨状态测试。

这些问题尚不能回答时，先明确并缩小目标和边界，再扩大实现范围。任何技术选择都应能够沿依赖关系追溯到真实需求。

## 12. 可执行知识、权威来源与历史材料

ATT 把目标、状态、权威事实、行动和验证证据组织成一套可执行规则，而不是把全部知识复制到单一入口。每项事实只有一个语义所有者；其他材料通过链接或明确引用来使用该事实。发现知识缺口时，先由责任方补充并验证事实，再让其他入口使用；缺失的契约不藏在编排代码或 Skill 中。

知识系统按职责分工：

- 本文件保存长期产品方向、架构判断方法，以及知识归属和同步要求；
- 现行规格保存已经确认的产品与外部接口契约；
- 指南解释如何调查事实、选择机制和组织验证，不重新定义规格；
- 代码和测试保存已经实现的行为与验证证据，不能反向覆盖现行规格；
- 代表性真实材料、权威说明、实际读写关系和验证记录证明领域事实与能力是否成立；
- Skill 是从使用者和执行者视角组织现行能力的可执行入口。它说明何时适用、应读取哪些权威材料、如何根据当前事实和状态选择行动，以及怎样判断完成、停止或恢复；它负责组织工作，但不重新定义产品契约和实现细节。

文档必须能够独立阅读和审查，不依赖某个 Skill 才能成立。Skill 根据当前目标和已有状态选择纵向切片，只读取当前任务所需的现行文档。任务确实需要跨阶段继续、交接或检查执行质量时，Skill 可以要求维护一份简短的实体任务清单；清单只保留稳定的宏观阶段和当前事实支持的工作，尚未发生的具体任务不提前列出。Skill 根据用户能够观察的证据决定继续、停止，或返回权威来源补充事实。

Skill 按触发、准备、状态选择、行动、验收和恢复组成一条连续、可以直接执行的流程。主入口能根据当前事实直接决定下一步，只按当前需要读取直接引用；同一个判断在一处完成，执行者不需要跨多个文件重新拼接。每项判断只在最需要的位置出现一次，条件、动作和结果都明确。只有某组内容具有独立职责并会在明确条件下单独读取时，才拆成引用文件或模板；尚未出现的场景不预设层级、抽象、状态、分支或产物。

除触发和导航所需的最短稳定摘要外，命令、参数、schema、字段矩阵、错误语义、完整协议、代码示例和详细领域规则都留在各自的责任方，Skill 只组织执行。领域中确实稳定的宏观阶段可以作为任务清单骨架；阶段内工作、检查方式和返工范围从当前项目事实产生，模板不预先固定它们。

面向执行者的知识不绑定具体模型产品或执行平台。需要指代参与方时，使用“使用者”“任务负责人”“执行者”或“协作者”等与平台无关的职责名称。是否引入协作者以及是否需要持久任务清单，由任务的连续性、交接和质量检查需要决定；专用 Skill 要求任务清单时，必须明确唯一写入者、创建时机、最小格式和更新规则。

统一知识入口只提供导航和编排；不同引擎表面相似的流程仍是各自的契约，各引擎的语义、判断和演进由相应纵向切片拥有。

领域模型、自然顺序、关联范围和边界行为，必须通过代表性真实材料、权威说明和实际读写关系共同验证。示例和测试夹具只用于解释已经确认的事实；字段、关系和命名先从真实材料中来，再进入领域模型。需要定位、继承或写回的原始字段、精确来源和未经授权的原值必须能够追溯。领域类型、注释和高层视图只用于帮助理解，权威事实始终来自材料本身。

版本控制历史是唯一允许保存当前系统旧版本材料的位置。当前工作树中的任何目录，包括 `archive/`，只保存当前有效的内容：旧实现、旧协议、旧格式、旧配置、旧测试和兼容方式都不在其中，也不被引用、加载、编译、识别或验证。

被替换或停止使用的 Skill 与工作流同样只属于版本控制历史，不是现行权威来源、模板、安装入口或兼容对象。`archive/` 只保存与当前系统版本无关的领域原始材料，用于验证当前领域事实和识别失败模式；当前系统的旧版本另有归宿。

历史中仍然可能适用于当前实现的知识，根据当前事实重新验证，并由当前责任方重新表达后，才能进入当前项目。

当前参数、临时命名、实现进度和候选细节进入对应的现行规格或代码文档，不进入本文件。

## 13. 完整发行物

`dist/` 是一个能独立工作的整体：目标平台上 ATT 的 `Release` 构建结果、程序直接读取的 `config.toml`、面向使用者的 `README.md` 和现行 `docs/`、可直接使用的现行 `skills/`、全部受支持语言的 `prompts/`，以及程序实际需要的非系统运行依赖和许可声明。实际运行的 `att.exe` 所在目录是唯一发行根：程序固定读取同目录的 `config.toml`、`projects/` 和 `prompts/`，不接受外部配置或资源根。使用者只靠发行包就能完成任务，Skill 也只使用同一发行目录中的程序、文档、Prompt、配置和项目；源码、构建目录和调用 cwd 不在发行资源搜索范围。发行内链接在包内闭合。`AGENTS.md`、开发导航、配置示例、源码、测试、构建缓存、维护者专用文档和历史版本文件属于开发环境，不进发行包。

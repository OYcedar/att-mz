# AGENTS.md

本文件记录当前项目长期有效的产品方向、架构判断规则，以及知识的归属和同步要求，确保后续开发始终服务同一个产品目标。

这里记录判断原则与知识分工，不记录当前命令、参数、模块名、数据字段、第三方库、完成状态或具体工作流。现行规格描述已经确认的外部契约，代码与测试描述已经实现的事实。

规则应直接说明遇到什么情况、如何判断和采取什么行动。对话中的具体例子只帮助识别职责、不变量和取舍；除非写入现行规格，否则这些例子不自动成为通用规则。本文件通过正面说明判断标准，而不是罗列历史禁令。

> 总纲：从真实用户结果反推纵向切片，从当前职责推导直接依赖；允许跨越物理层级，但不绕过负责相应规则、状态和不变量的模块（下文称“语义所有者”）；只设计当前目标及其直接依赖，根据已有调用方的实际需要复用模块和演进数据模型。

## 1. 产品形态：统一入口，独立纵向切片

项目的产品目标是以一个 CLI 聚合多个游戏引擎的翻译能力，同时让不同引擎保持彼此独立的命令域和纵向切片。统一入口只承担真正跨引擎一致的进程级职责和路由；每个引擎完整拥有自己的命令、参数、行为、错误语义和演进节奏。

表面相同的命令或流程不自动构成同一契约。各引擎先准确表达自己的用户意图，再在契约之下复用语义一致的能力。

面向用户的入口只表达本次操作意图，以及确实需要当场选择的事项。已经由项目状态或执行配置确定的事实直接复用。只有经过证据确认，并且确实属于部署环境、外部服务或操作者长期选择的策略，才由统一配置负责。固定的产品语义、内部资源参数、调度细节和运行时数据由负责执行它们的模块决定，不能变成每次操作都要重复填写的参数，也不能伪装成用户配置。

```text
[统一产品入口]
       |
       +----> [引擎纵向切片 A] ----+
       +----> [引擎纵向切片 B] ----+----> [共享能力网络]
       +----> [引擎纵向切片 C] ----+

纵向切片拥有外部契约和编排权；
共享能力只承载已经确认的一致语义。
```

一个纵向切片从用户入口一直延伸到用户能够观察的结果，包含产生该结果所需的业务判断、状态交接和副作用。项目进度以真实结果衡量，不以目录、模块、crate 或底层组件的数量衡量。

## 2. 从结果出发，自上而下推进

每项任务先明确它要完成的结果，再决定代码结构。开始实现前至少回答：

1. 谁在什么场景下需要这个结果；
2. 成功、失败和必须保持的行为分别是什么；
3. 哪个当前目标对结果负最终责任；
4. 当前目标真正需要哪些直接能力；
5. 本次任务在哪里停止，如何证明已经完成。

对于内部模块任务，以已确认的上游契约和模块自身应交付的行为作为起点；技术组件本身不是目标。

```text
[用户结果 / 已确认的上游契约]
                 |
                 v
             [当前目标]
                 |
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]

========== 当前设计停止线 ==========
```

当前任务只展开目标及其全部直接依赖。到达直接依赖的公开契约后停止，不递归设计它们的内部依赖。某个依赖进入后续任务时，把它作为新的当前目标，重新从其上游结果推导职责和直接依赖。

从入口到结果的任务可以逐步扩大实现范围，直到真实结果成立；每次扩大仍遵循同一推导过程，避免一次性设计整棵依赖树。

## 3. 契约先于实现

先从调用方视角定义承诺，再选择实现形态。一个契约应当说明：

- 调用方提交什么意图；
- 提供方返回什么结果；
- 成功和失败分别意味着什么；
- 哪些复杂性由提供方完整承担；
- 哪些行为是调用方可以长期依赖的。

契约先行不等于 trait 先行，也不等于模块或 crate 先行。函数、类型、模块公开接口或 trait 都可以表达契约；采用满足当前调用关系的最简单形式。只有确实存在替换、隔离、独立测试或多实现需求时，才引入相应的抽象机制。

接口由调用方的需要决定，以职责语言表达高级意图。算法、存储、协议、第三方类型以及资源限制和调度细节留在实现内部。

跨进程、外部服务或扩展调用同样遵循最小职责信息原则：只提供对方完成当前任务所需的有效事实，内部表结构、持久化身份、调度状态和恢复映射继续留在本方边界。外部协议描述“对方要做什么”，不把本程序如何实现这件事泄漏为对方必须理解的知识。

## 4. 依赖由语义责任决定

项目依赖是能力网络，不是必须逐层经过的阶梯。物理目录、架构图高度和“底层”标签不决定能否直接依赖。

当前模块可以直接依赖任何自己确实需要的稳定能力，包括位于更底层的核心能力；同时由真正拥有业务规则、状态和不变量的模块承接相应语义。

```text
合理的直接依赖：

       +----> [业务能力 B]
[A] ---+----> [业务能力 C]
       +----> [A 自己需要的核心能力 D]

需要经过语义所有者：

[A] ----> [B：拥有规则与状态] ----> [底层机制 D]
```

判断一条直接依赖是否成立时，依次确认：

1. 当前模块是否对该能力存在独立需求；
2. 调用是否只依赖对方职责级的公开契约；
3. 是否绕过了其他模块拥有的规则、状态或不变量；
4. 候选中间层是否增加了语义转换、策略、保护或生命周期责任；
5. 依赖方向是否保持单向并且能够避免循环。

当中间层只改名或转发调用时，直接依赖更准确；当中间层拥有不可替代的业务含义时，通过它协作。Composition Root（负责创建具体实现并把它们连接起来的代码）可以直接使用具体实现，以完成构造、配置和生命周期管理；业务执行仍通过相应契约完成。

外部入口和呈现层把业务执行交给相应责任模块，不反向成为其实现依赖。共享核心能力只提供已经确认的一致语义，不吸收特定调用方的规则；属于单一领域的底层能力可以保留该领域语义，并在当前确实需要时被直接依赖。

## 5. 模块边界与复用演进

模块围绕单一、可命名的职责形成。它负责与该职责相关的知识和变化，把内部状态、算法、第三方细节和复杂流程留在模块内部，对外提供高级接口，减少调用方必须理解的内容。

每次编写具体实现代码前，都必须检查现有实现能否复用；这项检查不只在新增游戏引擎时进行。新增纵向切片、实现直接依赖、补充根适配器、扩展既有模块或修复共享逻辑时，都先对照现有源码，分别作出以下判断：

1. 语义、规则、不变量、错误语义和生命周期一致的已有能力，直接复用；
2. 只有部分职责一致时，从既有实现中提炼最小、可命名且能独立验证的公共能力，由现有调用方和新调用方共同使用；
3. 仅代码形状相似，但业务语义、约束或演进方向不同时，明确记录差异并保持独立。

新增游戏引擎时，必须重新检查现有能力的语义、责任，以及当前有哪些调用方在使用它们。语义完全一致的能力，应在同一次改动中提炼为唯一的共享实现，并同步迁移全部现有调用方、配置、测试、规格和依赖图；语义不同的能力保持独立。

新增引擎不得通过复制现有纵向切片、在共享模块中累积引擎判断分支、增加只负责转发的中间层或保留平行实现来规避结构调整。只有全部受影响代码都改用最终的唯一实现后，新增引擎才算完成。

是否提炼共享能力，不要求整个模块完全一致。既有模块中的局部职责只要能够独立成立，并且不携带原调用方特有的规则，就可以提炼为共享能力。提炼时同步迁移现有调用方、测试、现行规格和依赖图；不先复制平行实现，也不同时保留新旧两套实现。复用的目标是消除重复的知识和机制，不是强迫不同业务经过同一流程，也不是为了形式上存在一个公共层。

拆分必须有明确的工程作用；过度拆分和重复实现都需要避免。不得仅因文件较长、目录整齐、层级对称、代码形式上可以拆开，或猜测将来可能复用，就新增模块、接口或 crate。

拆分后的单元只有满足以下任一条件时才成立：

- 具有独立、可命名的职责，有自己的不变量或变化原因，当前确实有调用方需要它，并且能够减少调用方必须理解的内容；
- 形成必要的依赖、编译、发布或所有权隔离。

共同维护同一不变量、总是一起变化，或者离开彼此就无法独立理解和使用的代码，应保留在同一模块或 crate 中。拆分不得产生只负责转发的中间层、迫使读者在过多文件间跳转、扩大公开接口、增加不必要的依赖或形成循环依赖。物理边界始终采用能够完整表达当前语义的最简单形式。

在检查复用和编写实现时持续判断：

- 这段知识由谁负责；
- 它是否形成可以独立命名和测试的职责；
- 当前实现是否混入了不属于本职责的知识；
- 当前调用方，或已确认的后续调用方，是否需要相同语义；
- 提炼后能否减少重复知识，而不只是增加调用层级。

发现可能复用的职责时，先让相关代码具有清晰、窄小的边界。共享接口只随实际需要扩大。实际需要既可以来自现有调用方，也可以来自已经确认、明确需要该能力的后续调用方；不要求先产生第二份重复代码。是否复用取决于语义是否一致，而不是代码形状、函数名称或处理步骤是否相似。

```text
[独立职责]
     |
     v
[切片内清晰边界]
     |  已有调用方需要相同语义
     v
[共享能力模块]
     |  需要独立依赖、编译、测试或发布
     v
[独立 crate]
```

围绕同一职责、共享同一组内部实现并适合共同编译和发布的 `mod` 放在同一个 crate。只有依赖隔离、编译边界、发布边界或所有权边界确实独立时，才提升为新的 crate。

共享能力组成按需连接的网络。每个纵向切片只选择自己需要的能力并保留编排权；共享层不能演变成统一控制所有业务、要求所有业务经过同一流程的框架。

## 6. 简洁表达、数据类型与配置

代码和数据设计追求“用尽可能少的概念完整表达当前真实语义”。简洁不是机械减少文件、类型或代码行，而是减少调用方必须理解的无效概念。每个类型、字段、状态、接口和中间层都应能够说明由谁负责、当前哪些调用方会使用，以及它消除了什么复杂性。后续需求出现时再逐步扩展现行模型，不用预留概念代替当前设计。

进程内部优先使用语言原生、所有权明确并能保证不变量的领域类型。互斥模式使用枚举表达；组合事实使用字段私有的值类型，并通过构造函数建立不变量，使非法状态尽量无法构造。通用文本树、字符串标签和松散键值只用于解析、序列化或持久化；进入内部前，应转换成已经校验并能保证不变量的类型。用于诊断的展示文本不能反过来成为权威标识或解析协议。

面向人类或模型执行者编写的外部格式也是产品接口。它应当易写、易读、表达力足够，并且适合大规模生成和审核；只保留完成当前意图所需的字段。需要表达复杂内容时，优先采用成熟、广泛理解的既有标准，并且只规定本项目实际读取和处理的语义。

业务模型、输入输出协议、持久化格式和配置不建立版本概念，不包含业务版本字段、版本枚举或版本分流，也不引入用于兼容、识别或迁移历史格式的旧字段别名或扩展层。只有当前实现确实读取或处理元数据和扩展内容时，它们才能进入契约。

面向 LLM 的 user message 只包含完成本次模型任务所需的最少内容，不复制内部领域模型、持久化结构或设想中的扩展协议。只发送模型生成当前输出不可缺少，并且尚未由 system message 或既有上下文确定的事实。能够根据顺序、现有分组或调用方已经校验的状态稳定推导出的内容，不得重复编码。

默认沿用最短、直接、易读的现有表达。只有当前任务确实需要无歧义的嵌套或转义，并且现有表达无法支持时，才可以改成 JSON。不得仅为统一格式、方便机器读取、便于调试或设想将来扩展而增加包装层、标签、空字段、重复计数或重复约束。

任何新增内容都必须说明模型当前如何使用它，以及为什么缺少它就无法正确完成任务。

外部松散字段只有在当前流程确实需要读取、写回或原样转发时才保留，并且只停留在相应边界。原样转发本身可以构成实际需要，但不能因此要求业务模块理解这些不透明字段，也不能因为猜测将来可能有调用方使用而扩大内部模型。

项目实行严格的单版本实现原则。现行规格是系统唯一有效的版本定义，每次版本迭代都以新规范一次性替换旧规范。迭代完成后，生产代码、测试、测试夹具、配置、现行规格、代码注释、错误类型、诊断、日志和用户提示只描述并实现当前版本。

不得以任何形式保留或重新引入旧版本，包括兼容、识别、检测、探测、专用拒绝、迁移、转换、回退、降级、警告、提示、条件分支、旧格式解析器、旧字段或旧值别名、双读双写，以及仅为证明旧版本行为而存在的测试和样本。

所有输入只依据当前规范解析和校验。不符合当前规范的内容统一作为普通无效输入处理，使用当前契约已有的通用错误语义；系统不得判断、记录或提示该输入可能属于旧版本，也不得通过版本号、特征字段、格式指纹或失败后的备用解析路径推断其历史身份。旧版本数据和调用方不构成当前实现的运行时场景，不能据此增加代码路径。

版本切换必须在同一次改动中删除旧实现及其全部残留，并同步更新当前调用方、数据定义、测试、配置、现行规格和依赖图；不采用过渡期兼容层，也不分阶段保留旧实现。

已有数据若不符合新规范，应在当前实现之外完成替换、重建或清理；不得在当前代码库中为其增加迁移器或专用识别逻辑。工具链要求的包版本或构建版本只能标识当前构建结果，不得参与业务分流、输入识别或历史版本枚举。

引入资源限制和调度机制前，必须先证明它们确有必要。只有外部服务配额、操作系统或协议的硬性限制、已经测量的性能瓶颈，或者能够复现并会导致故障的资源竞争，才能支持引入并发上限、队列、超时、批量、重试、等待窗口或容量拒绝。

配置已经存在、已有代码读取该配置、参数可以调节，或者某项机制看似能够保护用户电脑，都不能证明该机制合理。不得先实现机制，再要求配置为它提供数值。

证明机制必要后，再根据谁掌握相应事实来决定参数取值。部署环境、外部服务或操作者确实能够选择的策略进入统一配置；固定的领域事实和产品语义由代码契约规定；并行工作单元数量、调度窗口、传输批次、重排窗口、连接复用和缓存策略，由执行代码根据基准测试和运行时数据决定。

影响资源占用不等于必须交给用户配置，内部实现细节不得伪装成用户的业务选择。缺少必填的外部选择时应明确失败；没有实际选项的模块不创建空配置对象。

项目规模不是可配置策略，也不接受 ATT 自行规定的产品上限。不得对文件字节数、目录项数、目录深度、目录总字节、Lua 值大小或复杂度、SQLite 查询组数、行数或结果字节，以及 Claim、Unit、Group、Task 等业务总量建立人为上限、拒绝分支或对应配置。

“无上限”意味着不存在这项比较和拒绝逻辑，不是把默认值换成一个很大的常量。输入只能因真实的文件系统、操作系统、地址空间、内存、SQLite、外部协议或数据格式错误而失败。程序不得为了替操作者预防资源耗尽而提前拒绝规范内的输入，并且必须完整报告实际失败原因。

命名、类型和注释共同表达领域责任。命名优先回答“这是什么职责或事实”。注释重点解释业务含义、不变量、来源映射，以及仅从代码本身看不出的取舍；不重复代码表面动作，也不用臆造的语义名称覆盖需要保留的原始事实。

## 7. 语义所有权、信任与错误

每项业务事实、规则和不变量都有唯一责任方。最了解外部输入含义的边界负责解析和校验，并把输入转换成能够保证不变量的内部类型。转换成功后，内部模块按契约直接协作，不重复校验或重新解释同一事实。

```text
[外部事实]
     |
     v
[责任边界：解析、校验并转换为内部类型]
     |
     v
[能够保证不变量的内部数据]
     |
     +----> [内部模块] ----> [内部模块]
```

内部信任是对契约和已建立不变量的信任，不是忽略实际可能发生的失败。静态业务语义在所属边界建立一次；会随时间变化的外部状态，由真正执行副作用的模块在操作发生前重新观测。输入错误、运行环境失败和内部不变量破坏保持不同含义。

错误应由责任边界转换成调用方能够理解和处理的形式，同时保留定位问题所需的完整原因和上下文。产生具体错误的代码仍然掌握类型、阶段、路径、字段、数值、稳定错误码和提交最终状态时，就应构造一份已经按敏感信息规则处理的结构化错误记录。

CLI、日志和其他输出只读取这份记录，不在顶层对错误做 downcast（向下类型转换），也不解析 `Display` 文本或遍历任意错误对象来猜测事实。

错误和日志必须明确说明具体失败内容，以及已经按敏感信息规则处理的底层原因。只要相应事实存在，输出就应展示命令和阶段、路径或对象、实际值和期望值、OS/SQLite/HTTP 等稳定代码、底层原因、状态是否改变、处理办法和恢复位置。

责任域分类只能作为补充，禁止用“输入错误”“项目不可用”“运行失败”“内部错误”等泛化类别替代具体诊断。主错误、相关错误和清理错误不得互相覆盖，也不得因为队列压力而丢失。

敏感信息必须由现行规格通过封闭清单明确列出；任何模块、日志、诊断、文档或测试都不得按内容类别、用途或主观判断自行增加项目。清单成员、精确替换方式和内容边界只由 [Chat Completions 运行根规格](docs/runtime/chat-completions.md#6-敏感信息闭集唯一权威)规定。

其他文档和代码只引用该规定，不复述清单，也不派生自己的清单。隐藏清单中的值时，不得连带抹去它所在的字段、正文、阶段、路径、字段名、状态码、SQLite 或 OS 代码、实际值与上限、事务和发布最终状态。不得以保护敏感信息为由省略现行规格允许呈现的详情。

业务允许的未产出、部分结果和暂时不可用属于正常结果，不因结果不完整而变成程序错误；只有模块已经无法继续履行契约或保持状态一致时才返回错误。正常结果仍应包含足够的结构化诊断信息，并由统一负责日志和指标的模块记录，避免各业务模块自行拼接和分散保存日志。

日志、指标和追踪等可观测性记录默认只是业务行为的附属记录，不是权威业务状态。业务逻辑不得根据这些记录判断状态、决定是否提交、执行恢复或重放，也不得用它们决定是否允许产生副作用。普通的记录操作失败时，应产生已经按敏感信息规则处理且用户可见的诊断，但不能改变原业务结果、项目状态或退出码。

只有已经确认的用户结果、产品契约或外部约束明确规定“证据必须完整，操作才允许开始、成功或保持一致”时，相关记录才属于业务状态或具有权威性的审计记录。此时，现行规格必须明确哪些组件会读取这些记录、这些记录是否权威、如何持久化，以及记录失败意味着什么。记录内容重要、敏感、需要完整或需要长期保存，本身都不能证明它应成为业务操作的前置条件。

开放扩展能力时，必须先明确哪些输入可以信任，以及谁负责相应协议。扩展拥有自己的数据协议、生命周期、事务和跨阶段交接时，核心只向它提供约定的宿主能力和已经校验的上下文，不主动猜测、迁移或读取扩展的不透明产物。只有扩展明确采用核心契约时，双方才共享相应语义。统一产品入口不能以削弱扩展表达能力为代价，强迫所有数据进入同一标准流程。

可编程扩展、插件、脚本、自定义适配器和其他调用方需要把结果交给核心管理时，如果现有核心契约能够完整表达相同的语义、规则和不变量，调用方应把自己的私有表示转换成该契约接收的内部数据，由语义所有者在任何受管状态改变前统一完成验收、冲突处理和提交。调用方不得复制这些规则、绕过契约直接修改权威状态，或在后续阶段临时改写结果而形成多个权威来源。

现有核心契约无法表达调用方的真实语义时，允许调用方使用低级能力或保留私有协议，并完整承担相应的解析、校验、状态、一致性、事务、恢复、幂等和往返验证责任。核心可以提供可选的低级公共能力，但不能把没有经过核心契约验收的结果宣称为核心已经保证的受管状态。公共契约的作用是提供更安全的选择，不是限制扩展表达能力；多个真实调用方需要相同语义时，再按现有复用规则提炼最小公共能力并迁移相关调用方。

## 8. 状态与持久化来自真实交接

状态模型从真实纵向行为之间的交接产生。只有后续行为确实需要跨时间、跨进程或跨阶段保留的事实才进入持久化。

领域上下文的范围、耗时工作的分配单位、结果验收单位和持久化原子单位分别由各自的不变量决定，不要求彼此相同。相关事实可以作为整体提供上下文；能够独立确认的结果按最小有效单位验收；必须共同保持一致的传播或状态关系，则在同一个原子事务中提交。批量工作不自动意味着整批一起成功或失败。已经确认且保持一致的进展应及时保存；未完成部分由后续运行根据权威状态重新推导。

只有真实的领域关系、外部协议容量、已经测量的固定成本或吞吐瓶颈，才能证明批处理和流水线有必要存在。必须作为整体处理的语义单元不能拆开，相关上下文应尽量保留。可以把已经证实的固定成本分摊到更多工作上，但不能为了让批次看似装满而拼接无关内容，也不能在没有依据时切碎相关内容。

项目总工作量与同时处理的工作量必须严格区分。Claim、Unit、Group、Task、文档和 SQLite 行的总量不受内部窗口限制。流水线窗口只限制正在读取、计算、等待提交或执行外部请求的工作；窗口已满时，后续工作应等待，而不是失败。

内存可以用于索引、缓存、重排和保存已经完成的结果，以提高吞吐量。没有发生换页、内存耗尽或代表性负载问题的证据时，不得为了追求最低峰值内存而引入磁盘暂存队列、逐项消息往返或让整条执行路径串行运行。并发可以改变完成时间，但不能改变自然顺序、代表选择、分组结果或提交顺序。

```text
[当前行为]
     |
     | 后续行为必须继续使用的事实
     v
[持久化交接]
     |
     v
[后续行为]
```

每项持久化事实都应能够回答：

1. 谁负责写入；
2. 谁负责读取；
3. 它支持什么可观察行为；
4. 哪个位置是权威来源；
5. 它何时创建、更新和失效。

能够从权威事实稳定推导的数据在使用时计算。共享存储可以承载其他领域提供的信息，但解释权仍属于该领域。数据结构随已经确认的纵向切片小步演进，以真实读写关系证明每个字段存在的价值。

持久化结构优先直接表达领域资产及其真实读取、更新和事务边界。共同构成语义整体并经常一起读取的事实应保持明确关联；需要独立继承、校验和写回的叶子保留精确身份。只有出现真实的跨领域统一读写需求时，才引入通用中间模型，避免以更通用但表达力更弱的结构替代清晰的领域关系。

## 9. 性能、并发与资源限制

性能是核心产品契约和完成条件，不是正确性完成后可做可不做的优化。选择 Rust 的价值必须体现为：大型项目上的实际等待时间（wall-clock time）更短、能够有效使用多个 CPU 核心、减少重复工作，并提供可预测的原生执行。“使用了 Rust”“使用了异步或 Rayon”或“增加了线程”都不能替代实测结果。

### 9.1 何时验证性能

是否需要验证性能，取决于改动是否影响核心执行路径或某项已经说明作用的性能机制，不取决于改动行数、文件数量或提交规模。

以下改动不运行性能基准：不改变核心执行路径的文档和本地化改动、性能基准不读取的普通测试夹具，以及不在常用执行路径上的诊断代码改动。如果性能测试夹具、测量脚本或代表数据发生变化，必须重新确认测量流程和既有基线是否仍然适用。

改动涉及以下内容时，先通过静态复杂度分析或只测量受影响部分来判断风险：

- 算法复杂度或数据流；
- 并发和同步；
- 存储访问和文件扫描；
- 常用执行路径上的日志或序列化；
- 外部调用。

只有这些改动可能产生可测量的影响，或者已经观察到性能差异时，才与固定的基线进行正式比较。探索中的实现先验证受影响的热点；只有准备保留的最终实现才在真实游戏上完成验收。

没有证据时，不得删除已有并行处理、增加串行等待点、重复扫描或解析、增加磁盘中转、改成逐项事务、要求每项写入都立即持久化到磁盘，或者引入队头阻塞。

### 9.2 测量环境

需要性能验证时，MV/MZ 使用测试集中规模最大且结构最复杂的真实游戏，Generic 使用大型
JSONL 样本。只影响一个引擎时测对应主样本；影响公共核心路径时两类主样本都要测。在被
Git 忽略的 `tmp/` 下建立独立的测试工作区，并保存测量脚本、原始记录和报告。

测量必须使用通过 `Release` 配置和 MSVC 工具链构建的实际可执行文件，并为每次完整执行准备全新的来源副本、项目工作区、数据库和输出目录。不得复用前一轮 `Extract`、译文或 `WriteBack` 结果，也不得通过以下方式缩短时间：

- 使用 `unchanged` 快速路径；
- 预热应用自身的缓存；
- 减少提取覆盖；
- 跳过校验；
- 降低持久化或发布保证。

操作系统缓存必须受到控制，并在报告中说明处理方式。现行语义要求在单次运行内执行的全局去重，不属于利用缓存缩短测量时间。

### 9.3 比较轮次与停止条件

使用受影响领域中最大且最复杂的真实样本选择最终实现。公共能力同时使用最大的真实
RPG Maker 样本与大型 Generic JSONL 样本。其他代表样本各做一次成对回归，只用于发现
明显退化或样本特有错误，不把不同游戏或两类引擎样本当作同一统计分布的重复样本。

正式比较先运行两轮成对交错测试，第二轮交换两个实现的先后顺序。如果两轮方向一致，并且结果已经明确达到接受或拒绝条件，立即停止。只有结果冲突、接近判断条件，或者存在能够说明的环境异常时，才运行第三轮。第三轮后仍无法判断时，保留现行实现并记录证据不足；不得自动增加更多轮次。

开始测试前，必须根据预检的实际耗时说明：

- 剩余测试预计需要的总时间；
- 最多运行多少轮；
- 哪些结果会让测试提前停止；
- 整个测试允许使用的总时间。

达到接受或拒绝条件、发现正确性差异，或者下一轮会超出时间预算时，立即停止。需要增加预算时，必须先说明额外时间能够改变什么决策，并取得使用者明确同意。

常规的两至三轮测试只报告每轮原始值、成对差值、中位数和范围，不得声称得到 p90、p95 等尾部分位数。详细流程只由 [性能验证指南](docs/performance-validation.md) 规定。

### 9.4 性能目标

在固定的基准机器和上述最大真实游戏上，三项核心路径的目标分别是：

- `Extract`：尽量把总耗时压缩到 10 秒以内；
- `Translate`：排除模型供应商实际生成内容和网络等待的时间后，把项目读取、预处理、全局去重、任务构造、响应验收、传播和持久化的总耗时压缩到 10 秒以内；
- `WriteBack`：把完整资产读取、引擎改写、候选构建、验证和单次发布的总耗时压缩到
  10 秒以内。

测量 `Translate` 中 ATT 自身耗时时，使用能够立即返回确定且合法结果的本地 `Provider`，但不得跳过任何真实任务或提交工作。任何一个阶段的耗时还未降到 10 秒以内时，都不得宣称性能工作已经完成；必须保留各阶段的测量结果、瓶颈证据和下一步仍需优化的内容。

### 9.5 正确性条件

不得用逻辑错误、减少行为或降低核心能力来换取性能。以下行为都是不可放宽的验收条件：

- 提取覆盖和自然顺序；
- 指纹和全局去重；
- 代表项与 `seed` 选择；
- MV/MZ Mutation Claim 冲突与 Generic 动态输入一致性；
- 任务内容和编号；
- 事务原子性和部分翻译进度；
- MV/MZ Rules、Generic JSONL 同步，以及改动原子数据库 Lua 时的事务语义；
- 取消和候选清理；
- 完整验证和单次目录发布。

输出或状态存在任何逻辑差异时，性能成绩无效。发现核心路径中存在超线性算法、重复工作、错误状态或不一致行为时，必须修复后再交付，不能把它们作为已知问题保留。

### 9.6 并发方式与参数

根据工作负载选择并发方式。等待型 I/O 使用能够在窗口已满时让上游等待的异步处理，或使用专门执行阻塞操作的资源；CPU 密集型工作使用受控的数据并行。

独立文档、Group、规则、任务和响应默认可以并行处理，或者与相邻阶段重叠。只有存在明确的数据依赖、必须按确定顺序合并结果、单写者事务、单个 VM 或最终发布边界时，才可以保持串行。串行范围必须尽量短，并说明无法并行的具体原因；不能仅以“保持顺序”为由让全部计算串行执行。

异步和多线程设计必须明确哪些工作会同时运行、窗口已满时如何等待、如何取消、错误如何传递，以及每个任务何时结束。内部并行工作单元数量、窗口大小和批次大小按以下方式选择：

1. 在最大真实样本上，用限定数量的对照实验比较不同参数；
2. 通过预检排除明显较差的参数；
3. 只让最有希望的参数进入两轮成对比较；
4. 选择吞吐量已经接近最高、继续增加资源也几乎没有收益的一组参数。

不得运行所有参数与所有样本的完整组合。内部参数由执行代码根据基准测试或运行时数据决定；只有供应商并发限制、RPM 和网络超时等真实外部约束进入配置。并发不得破坏结果确定性、持久化一致性或错误语义。

### 9.7 优化与验证顺序

性能优化先用分阶段计时、算法复杂度分析和必要的性能分析器（profiler）证明瓶颈，再针对已经证实的瓶颈实施。正确性验证随候选实现的成熟程度逐步扩大：

1. 如果固定基线的构建结果、源码、配置和既有验证记录都没有改变，可以复用已经记录的完整验证；没有适用记录时，只补做一次；
2. 探索中的实现开始计时前，只运行受影响的专项测试、相关契约测试和结果等价检查；因静态检查或性能不足而淘汰后，不补跑全部验证；
3. 只有准备保留的最终实现运行一次完整工作区测试、完整契约矩阵和失败路径验证；
4. 如果候选实现改变了相应的外部契约或关键语义，在计时前先扩大受影响的验证范围。

每轮计时都必须重复执行真实受影响路径，并核对退出码、结果哈希和必要的最终状态。任何结果不等价都会使该轮性能成绩失效。

CPU、磁盘、同步、事务和峰值内存等详细数据，只在定位瓶颈或验证相应资源假设时单独采集；不要求每个候选、游戏和轮次都采集。不得只展示单次最好成绩或缓存命中的结果，也不得用无法量化收益的复杂设计换取性能。核心路径相对固定基线出现严重性能下降时，不得交付。

如果某项性能下降确实无法避免，必须提供分阶段测量和性能分析记录，并说明：

- 新增成本对应什么必要语义或外部硬性限制；
- 已经尝试过哪些优化；
- 为什么在不破坏正确性和产品契约的前提下无法继续降低成本。

“实现更简单”“改动更统一”或未经验证的推测不能作为解释。真实测试集和 `tmp/` 中不纳入版本控制的测量脚本，可以根据当前风险直接调整，不为旧测量材料保留兼容处理。

## 10. 实现、验证与完成定义

当前版本的用户可观察契约、持久数据定义和现有外部调用方，决定本次改动必须同步更新哪些内容，但不产生对旧版本的兼容责任。版本迭代一经确认，旧契约、旧数据格式和旧调用方就退出当前范围；不得因为它们曾经存在而保留兼容、识别、拒绝、迁移或提示逻辑。

既有结构无法准确表达当前语义、产生重复知识或持续阻碍实现时，应进行范围明确的破坏性重构。在同一次改动中更新全部当前调用方、测试、配置、现行规格和依赖图，彻底删除旧实现；不使用转接层、新旧两套模型或只处理历史版本的错误类型来保存已经失效的设计。

继续实现直接依赖时，除根接口外，所有包含领域判断、状态转换或编排规则的能力都应获得真实实现。最终的根接口只负责文件、计算、存储、网络、时间和运行时等环境机制。Composition Root 统一负责转换外部配置、创建具体实现并管理其生命周期。

以下内容代表不同的完成程度，交付时必须分别说明：非根业务算法已经成立、根接口已经定义、根适配器真实可用，以及生产入口已经连接这些实现并能完整执行。

实现顺序与设计顺序保持一致：

1. 用高层行为固定当前目标的可观察结果；
2. 根据调用方的需要定义直接依赖的最小契约；
3. 实现并验证当前目标，范围外依赖使用测试替身；
4. 某个依赖进入实现范围后，再以其上游契约为起点实现并验证；
5. 任务要求从入口运行到结果时，用最小真实样本完整执行这条路径；
6. 共享能力有多个调用方后，验证各方依赖的是同一语义。

依赖暂未实现时，可以使用测试替身验证当前目标。生产入口只连接已经具备真实实现并完成相应验证的能力。只有契约、局部测试或底层组件已经完成，都不能单独证明纵向切片已经完成。

当前任务完成，意味着约定范围内的结果或上游承诺已经成立，失败语义明确，并且已经完成相应验证。完整纵向切片完成还要求：用户结果能够通过实际生产入口获得，必要状态能够正确交接，相关副作用和资源边界已经验证。代码、文档和交付说明必须明确标出尚未实现、尚未连接或尚未验证的范围。

架构图、现行规格和测试随已经实现并验证的事实同步更新。图只描述真实依赖或明确标注的候选设计，不能把设想写成现状。

文档中描述的已实现行为、职责边界、调用关系、入口、约束、示例和路径，必须能够由当前源码和测试验证。经确认的当前契约或实现发生变化并使这些说明失效时，必须在同一次改动中修正文档，并使用与源码一致的职责名称和含义。源码、测试和现行规格发生冲突时，先确认当前有效契约，再修正不符合契约的一方；不得仅为解释现有代码而改写规格。尚未实现的内容应明确标为设计目标，不能与现状混写，也不能要求维护者通过比对源码猜测哪一处说明已经过期。

开始实现前，先从预期的外部结果反查源码、测试、现行规格、指南、Skill 和发行物，将它们纳入影响检查范围，并把确认受影响的内容纳入实施范围。任务说明没有逐项点名某类材料，不能作为不检查或不更新的依据。

从使用者或执行者的外部视角能够观察到的功能、行为或流程经确认发生变化时，必须在同一次改动中更新负责相应语义的全部内容：

- 能力范围、输入输出、权限、副作用、环境要求、成功失败或恢复语义变化时，同步更新现行规格和测试；
- 调查事实、选择能力、组织操作或验证结果的方法变化时，同步更新指南；
- 任何外部功能或流程变化都必须检查覆盖该场景的现行 Skill；变化会影响执行者的适用范围、前置条件、必读材料、状态选择、操作顺序、授权与副作用、完成证据、停止条件或失败恢复时，同步更新 Skill 包中负责相应流程的内容；
- 发行内容或使用入口变化时，同步更新发行资源、导航和发行验证。

执行者的阅读、选择、授权、操作、验收或恢复方式已经变化时，只更新 Skill 引用的现行文档不能替代 Skill 自身的更新。现行 Skill 已经完整覆盖变化，并且上述执行判断均未改变时，不复制产品事实或制造文件差异，但必须在交付中说明影响检查的结论。仅有内部实现变化且外部行为没有变化时，同样不机械修改 Skill。

源码与测试、现行规格与指南、受影响的 Skill 和发行物必须共同描述同一个当前版本。这里的一致是事实、名称、行为和流程一致，不是把同一内容逐字复制到多个位置。任何一处仍描述旧行为、遗漏当前流程、存在已知冲突、失效链接或过期导航时，任务尚未完成。只有长期产品方向、架构判断方法、项目边界，或者知识归属和同步规则发生变化时，才更新本文件。

## 11. 每次任务的方向检查

设计、实现和评审都使用以下问题检查方向：

1. 当前工作最终服务什么用户结果或上游承诺；
2. 谁拥有该结果及其中的规则和不变量；
3. 当前目标的直接依赖分别是什么，为什么是直接依赖；
4. 是否存在只负责转发、没有增加规则或保护的中间层，或者绕过语义所有者的调用；
5. 新模块抽取的是稳定职责还是未经证明的未来设想；
6. 编写具体实现前是否已经检查现有完整能力和局部职责，并说明哪些直接复用、哪些提炼共享、哪些因语义不同而保持独立；
7. 候选拆分是否具有独立职责和实际收益，还是只让文件更短、目录更整齐或层级更对称；拆分后是否反而产生转发层、迫使读者在更多文件间跳转、扩大公开接口或增加不必要的依赖；
8. 外部事实在哪个边界完成校验并转换成能够保证不变量的内部数据；可编程扩展把结果交给核心管理时，是否复用了语义所有者的现有契约；无法复用时，是否明确承担了私有协议的完整责任，并避免把未经核心验收的结果当作核心受管状态；
9. 新增状态是否有明确的写入方、读取方和行为用途；
10. 哪些证据能够证明纵向结果，而不只是证明局部代码可运行；
11. 每个并发上限、队列、超时、批量、重试、等待窗口、容量拒绝或磁盘暂存队列，分别由什么外部硬性限制、可复现故障或测量证据证明；是否错误地把“已有配置”“已有代码读取”或“可以保护用户电脑”当成必要性证据；
12. 内部模型是否用强类型直接表达不变量，外部格式是否保持最小且易于大规模书写；
13. 已经证明必要的策略，为什么分别由外部配置、代码契约或执行代码根据运行时数据决定；是否错误地把并行工作单元数量、队列、批次和缓存交给用户配置；
14. 是否对项目总量建立了任何 ATT 自行规定的文件、目录、Lua、SQLite、Claim、Unit、Group 或 Task 上限；所谓无上限是否真正删除了比较和拒绝路径；
15. 各输出是否明确展示已经按敏感信息规则处理的底层原因、阶段、路径、稳定错误码和最终状态；是否用泛化类别或保护敏感信息为由省略了允许公开的详情；
16. 新增的日志、指标或追踪记录是否仍然不参与业务判断；如果记录失败会改变业务结果或项目状态，哪些现有组件会读取它，以及哪项已确认契约证明它属于业务状态或权威审计记录；
17. 批处理、流水线、验收和提交单位是否分别符合领域关系、实测成本与一致性要求；处理窗口已满时是否只让后续工作等待，而不限制总工作量；
18. 改动是否影响核心执行路径，并能指出可能产生可测影响的算法、并发、存储、序列化、
文件、同步或外部调用机制；若有，是否先只测试受影响部分来筛选候选，并且只让准备保留
的最终构建结果按影响范围使用测试集中最大、最复杂的真实 RPG Maker 游戏与大型 Generic
JSONL 样本，在 `tmp/` 的全新状态下完成没有缓存捷径的 `Release` 验收，持续逼近或达到
`Extract`、`Translate`、`WriteBack` 各自 10 秒的目标；
19. 性能验证是否在开始前说明总时间预算、最多轮数和提前停止条件；是否错误地把不同游戏当成同一分布的样本计算分位数，或者在证据已经足以接受、拒绝或保持现状后仍继续增加轮次；
20. 性能结果是否保持全部核心语义且没有严重下降；无法避免的性能下降是否有测量、性能分析、必要语义和已尝试优化的完整证据，而不是通过减少工作、降低保证或主观解释来掩盖；
21. 新增领域事实是否已经由真实样本、权威说明或当前调用方的实际使用验证；
22. 版本迭代后是否已经从生产代码、测试、夹具、配置、现行规格、注释、错误和诊断中删除旧版本的全部痕迹，并确保不符合当前规范的输入只按普通无效输入处理；
23. 源码与测试、现行规格与指南、受影响的 Skill 和发行物是否描述同一当前功能与流程；外部功能或流程变化是否已经检查覆盖该场景的 Skill，受影响的 Skill 是否在同一次改动中更新，无需修改的结论是否有明确依据；Skill 是否仍只组织执行并按需读取权威事实，没有复制契约或绑定特定执行平台。

这些问题尚不能回答时，先明确并缩小目标和边界，再扩大实现范围。任何技术选择都应能够沿依赖关系追溯到真实需求。

## 12. 可执行知识、权威来源与历史材料

ATT 把目标、状态、权威事实、行动和验证证据组织成一套可执行规则，而不是把全部知识复制到单一入口。每项事实只有一个语义所有者；其他材料通过链接或明确引用来使用该事实。发现知识缺口时，先由责任方补充并验证事实，再让其他入口使用，不能把缺失的契约藏在编排代码或 Skill 中。

知识系统按职责分工：

- 本文件保存长期产品方向、架构判断方法，以及知识归属和同步要求；
- 现行规格保存已经确认的产品与外部接口契约；
- 指南解释如何调查事实、选择机制和组织验证，不重新定义规格；
- 代码和测试保存已经实现的行为与验证证据，不能反向覆盖现行规格；
- 代表性真实材料、权威说明、实际读写关系和验证记录证明领域事实与能力是否成立；
- Skill 是从使用者和执行者视角组织现行能力的可执行入口。它说明何时适用、应读取哪些权威材料、如何根据当前事实和状态选择行动，以及怎样判断完成、停止或恢复；它负责组织工作，但不重新定义产品契约和实现细节。

文档必须能够独立阅读和审查，不能依赖某个 Skill 才能成立。Skill 根据当前目标和已有状态选择纵向切片，只读取当前任务所需的现行文档。任务确实需要跨阶段继续、交接或检查执行质量时，Skill 可以要求维护一份简短的实体任务清单；清单只保留稳定的宏观阶段和当前事实支持的工作，不提前列出尚未发生的具体任务。Skill 根据用户能够观察的证据决定继续、停止，或返回权威来源补充事实。

Skill 应按触发、准备、状态选择、行动、验收和恢复组成一条连续、可以直接执行的流程。主入口应能根据当前事实直接决定下一步，只按当前需要读取直接引用；不得要求执行者跨多个文件重新拼接同一个判断。每项判断只在最需要的位置出现一次，条件、动作和结果都应明确。只有某组内容具有独立职责并会在明确条件下单独读取时，才拆成引用文件或模板；不得为尚未出现的场景预设层级、抽象、状态、分支或产物。

除触发和导航所需的最短稳定摘要外，Skill 不复制命令、参数、schema、字段矩阵、错误语义、完整协议、代码示例或详细领域规则。领域中确实稳定的宏观阶段可以作为任务清单骨架；阶段内工作、检查方式和返工范围必须从当前项目事实产生，不能由模板预先固定。

面向执行者的知识不绑定具体模型产品或执行平台。需要指代参与方时，使用“使用者”“任务负责人”“执行者”或“协作者”等与平台无关的职责名称。是否引入协作者以及是否需要持久任务清单，由任务的连续性、交接和质量检查需要决定；一旦专用 Skill 要求任务清单，必须明确唯一写入者、创建时机、最小格式和更新规则。

统一知识入口只提供导航和编排，不把不同引擎表面相似的流程提升为共同契约；各引擎的语义、判断和演进仍由相应纵向切片拥有。

领域模型、自然顺序、关联范围和边界行为，必须通过代表性真实材料、权威说明和实际读写关系共同验证。示例和测试夹具只用于解释已经确认的事实，不能先凭空创造字段、关系或命名，再用它们决定领域模型。需要定位、继承或写回的原始字段、精确来源和未经授权的原值必须能够追溯。领域类型、注释和高层视图只用于帮助理解，不能覆盖权威事实。

版本控制历史是唯一允许保存当前系统旧版本材料的位置。当前工作树中的任何目录，包括 `archive/`，都不得保留当前系统的旧实现、旧协议、旧格式、旧配置、旧测试或兼容方式，也不得引用、加载、编译、识别或验证这些内容。

被替换或停止使用的 Skill 与工作流同样只属于版本控制历史，不能作为现行权威来源、模板、安装入口或兼容对象。`archive/` 只保存与当前系统版本无关的领域原始材料，用于验证当前领域事实和识别失败模式；它不能存放当前系统的旧版本。

历史中仍然可能适用于当前实现的知识，必须根据当前事实重新验证，并由当前责任方重新表达后，才能进入当前项目。

当前参数、临时命名、实现进度和候选细节进入对应的现行规格或代码文档，不进入本文件。

## 13. 完整发行物

`dist/` 必须包含目标平台上 ATT 的 `Release` 构建结果、程序直接读取的 `config.toml`、面向使用者的 `README.md` 和现行 `docs/`、可直接使用的现行 `skills/`、全部受支持语言的 `prompts/`，以及程序实际需要的非系统运行依赖和许可声明。发行包必须能够独立支持使用者完成任务：Skill 默认使用同一发行目录中的程序、文档、Prompt 和配置；用户明确指定外部配置时，只使用该配置按产品规则解析出的 Prompt 路径，不搜索源码或构建目录。发行内链接必须在包内闭合。发行包不包含 `AGENTS.md`、开发导航、配置示例、源码、测试、构建缓存、维护者专用文档或历史版本文件。

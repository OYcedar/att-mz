# AGENTS.md

本文件只规定当前项目特有的目标、架构方向和协作约束。通用的语言、工程、验证与交付规则沿用上层 `AGENTS.md`；发生冲突时，以更具体且较新的用户要求为准。

## 1. 项目目标与当前范围

本项目是一个以 Rust 实现的多游戏引擎 CLI 翻译器聚合项目。对用户只提供一个 CLI，目标命令形态为 `att <engine> ...`，例如 `att mz`、`att unity`、`att mv`、`att ue5`。不同引擎不是各自独立的产品；它们复用已经由真实用例验证的应用能力和翻译语义，并按自身需要组合阶段，不强迫所有引擎经过完全相同的流程。

当前第一个纵向切片是 `att mz`。按用户可观察流程，自上而下依次包含：

1. 初始化：建立项目数据库，只创建当前流程必需的最小表结构，并录入项目元数据。
2. 提取文本：从 MZ 固有文本位置、用户导入规则指定的位置、Lua 脚本返回的位置提取原文，并把原文及写回所需的最小定位信息存入数据库。
3. 翻译：从数据库读取待翻译原文，切分和组装有界任务块，以受控并发调用 LLM；把 LLM 响应作为外部输入，按照源语言和目标语言要求完成校验后再持久化译文。
4. 写回：读取已验收译文，按已保存定位写回原文位置，并产出可验证的结果。

MZ 游戏多数可直接读取明文数据，因此解包与封包不属于首个切片。以后确有加密或发行格式需求时，把它们作为相应引擎提供的可选能力加入，不得提前建造通用解包/封包框架。

生产运行路径使用 Rust。不得再次引入 Python 解释器、PyO3/maturin、Python sidecar、跨语言 JSON FFI、双语言共享领域模型或需要两套运行时同步发布的设计。Python 只可用于不进入产品运行路径的一次性开发工具；Lua 是已确认的文本提取扩展边界，不等同于允许第二套核心实现。

## 2. 当前事实源与历史归档

根目录中的现行代码、测试、文档和配置才是当前事实源。开始任务时先检查根目录现状；实际不存在的 Cargo workspace、crate、数据库 schema 或正式 CLI 契约均视为尚未决定，不得从目录名或历史版本推断它们已经确定。

`archive/` 中的失败项目只用于复盘，不参与构建、测试、发布和新设计：

- 不得复制其 crate 划分、接口、数据库字段、状态机或质量门禁作为新项目默认方案。
- 不得为了兼容归档代码而改变新架构。
- 可以引用其中真实游戏格式知识，但必须在当前用例中重新验证，并通过新的引擎边界进入系统。
- 归档文档不能证明当前能力已经存在。

## 3. 强制采用自上而下的任务设计

每个编程任务都从用户可观察结果开始，禁止先实现数据库、磁盘 IO、日志、网络、Lua 运行时等底层能力，再寻找上层用途。

开始编码前，必须依次完成：

1. 写清本次任务的用户入口、成功结果、失败结果、必须保留的行为和明确不做的内容。
2. 识别负责该结果的最高层模块，并画出它实际需要的直接依赖；只画当前用例能够证明的依赖。
3. 从最高层调用方的需要出发，定义每个依赖的最小接口，包括请求、结果、错误和必要语义。
4. 先实现最高层模块。依赖不在本次范围时，可以使用测试 fake 或内存实现验证其行为；用户目标包含端到端完成时，可以在同一任务中按已经定义的接口逐层实现真实依赖。
5. 后续任务选择一个尚未实现的依赖，把它视为新的最高层目标，重复同一过程。

例如 A 依赖 B、C、G 时，应先根据 A 的用例定义 B、C、G 对 A 承诺的最小接口，再实现和测试 A；不得先把 B、C、G 乃至它们的依赖 D、E、F 全部造完。接口先行的对象是上层用例，不是无人调用的底层库。

依赖接口原则上由消费方的需求拥有。“拥有”是语义所有权，不要求 trait 物理放在用例实现 crate；port 与边界类型可以放在应用拥有的 contracts 模块或 crate，使应用实现和适配器都单向依赖它，contracts 本身不依赖任何适配器，只有组合根依赖双方。

接口可以是私有函数、具体类型、模块门面、请求/结果类型或 trait。只有存在真实替换、运行时选择或外部副作用边界时才引入 trait；为了测试制造 fake 本身不是拆 trait 或 crate 的理由。接口不得照抄某个候选实现的完整 API，也不得泄漏 SQLx、HTTP SDK、Lua VM、引擎 JSON AST 等实现类型。接口尚无生产实现时不得注册为生产可用命令，fake/stub 不得接入生产组合根；也不得用 `todo!()`、`unimplemented!()`、空成功、虚构数据或静默降级把未实现能力伪装成已完成。

## 4. 模块与 crate 的划分

模块必须把属于自身职责的复杂性留在内部，对外暴露能够直接表达调用方意图的高级接口。外部不应为了完成一个业务动作而拼接模块内部的底层步骤、资源字段或状态转换。

独立职责和内部复杂性决定是否封装成模块；即使只有一个调用方，引擎专用解析或复杂第三方适配也可以且应当私有模块化。是否把它提升为共享模块，则由现实复用性决定。每次新增代码时都要判断：

- 这段逻辑是否表达独立且可命名的职责，而不是当前函数的偶然步骤？
- 它是否与具体引擎、当前命令或某个第三方库无关？
- 当前其他模块是否需要它，或能否明确指出下一类引擎会以相同语义需要它？
- 抽取后是否减少重复知识，并形成更小、更稳定的接口？

能够回答清楚时，应及时提升为共享模块，避免相同规则散落到多个引擎或阶段。所谓“未来可能复用”必须能指出潜在消费方和相同语义；只因名字看起来通用，不得创建空壳框架、万能 trait 或配置项。A、B、C 等依赖节点表示职责边界，不预先等同于 Rust `mod` 或 crate。

围绕同一个职责、共享同一组内部实现，并且适合一起编译和发布的 `mod` 放在同一个 crate。出现以下情况时才考虑拆 crate：

- 需要用编译依赖强制隔离方向；
- 能被多个上层或引擎独立复用；
- 依赖集合、运行环境、测试方式或发布节奏明显不同。

不得一个 `mod` 对应一个 crate，也不得把所有“通用”代码塞进 `common`、`utils`、`core` 或万能 IO crate。“都属于 IO”不构成单一职责；扫描/选择、内容编解码、原子写入/恢复、转移、清理、调度/背压等能力必须分别由当前上层用例证明需要。文件行数只是信号，职责和依赖方向才是边界；一旦同一模块拥有多个独立变化原因，应按职责拆分。非测试、非生成的生产源文件接近 800 行或一个模块接近 2,000 行时暂停扩写并复核职责；阈值只触发检查，机械拆文件不能算完成模块化。

公开面默认最小化。优先私有或 `pub(crate)`；只有真实生产调用方需要且契约已经稳定时才使用 `pub`，测试和文档本身不算调用方。公开 API、配置旋钮、状态、指标和错误类别都必须有当前生产消费方；配置只有在调用方当前需要选择且不同值会影响用户行为时才公开，否则先保留为模块内部默认值，并在测量证明需要后再开放。

## 5. 多引擎边界与依赖方向

共同翻译流程只拥有跨引擎成立的业务概念，不得出现 MZ/MV 事件编号、Unity 资源类型、UE5 包格式或某个引擎的文件路径约定。

具体引擎实现拥有：

- 项目识别与支持范围；
- 固有文本位置及其解析；
- 用户规则和 Lua 提取结果如何映射到该引擎；
- 写回定位的解释与文件修改；
- 该引擎确实需要时的解包和封包。

CLI 命令处理只解释用户意图并调用应用用例。除二进制组合根或专门的引擎注册层外，CLI 命令处理、应用用例、翻译和共享持久化模块都不得依赖具体引擎；组合根是唯一可以同时依赖应用实现与具体适配器并完成接线的位置。通用领域类型不得定义在具体引擎模块中。增加新引擎可以增加适配代码和注册入口，但不应迫使共同翻译流程、翻译任务语义或已有引擎进行大重构。若第二个引擎暴露出错误抽象，应修正共享边界，而不是让新引擎伪装成 MZ 数据。只有 MZ 一个实现时，只能声明 MZ 纵向切片可用；至少接入第二个差异明显的真实引擎后，才能宣称跨引擎抽象已经得到验证。

写回定位必须支持“提取 -> 退出程序 -> 重新启动 -> 写回”的完整往返。引擎拥有定位的语义和编解码；共享持久化可以不透明保存，或者由引擎使用自有表，但不得解释引擎专用字段。项目的引擎身份必须阻止错误适配器解释定位。具体载体由首个真实纵向用例决定，不得现在预设通用 JSON blob、万能定位 trait 或预留字段。

推荐的概念依赖方向是：

```text
单一 CLI / 组合层
        |
        v
应用用例（初始化、提取、翻译、写回）
        |
        v
由应用需要定义的窄接口
        ^
        |
引擎、数据库、LLM、Lua、文件系统等具体实现
```

上图是依赖原则，不是预先批准的 crate 清单。只有具体用例证明边界后才固化 workspace 结构。

## 6. 外部校验一次，内部按契约互信

每项来自外部的静态不变量只有一个明确 owner，并在具备足够上下文的最早边界校验一次。语法解析、跨字段业务语义和副作用安全可以由不同 owner 分别负责；同一不变量一旦转换为受信内部类型，下游不得重复校验。当前外部边界包括：

- CLI 参数、配置、环境变量和用户输入；
- 游戏文件、导入规则和其他外部文件；
- Lua 脚本及其返回值；
- LLM、HTTP 服务和其他进程的响应；
- 新接管、导入或版本不明的数据库。

边界模块负责解析、校验并转换成能够表达不变量的内部类型。转换成功后，内部模块必须传递并信任该类型和上游契约，不得把它降级成 `String`、`Path`、数字或松散 DTO，再由 A、B、C 各层重建并重复检查同一字段、语言、范围或状态。数据库 schema/version 兼容只证明存储形状，不证明数据库未被修改、游戏文件未变化或当前调用仍有写入权限；跨进程重新接管持久化定位时，应在边界完成一次结构和项目归属校验。

“内部互信”不表示忽略错误：

- 文件、数据库、网络和系统调用仍应传播真实运行失败；
- 无法用类型表达的模块前置条件应写进接口契约，由调用方保证；
- 内部不变量被破坏属于程序缺陷，应显式失败，必要时使用断言，而不是修补、默认或吞掉；
- 安全边界和副作用范围仍由真正拥有该边界的模块检查；路径越界、目标身份、覆盖确认和事务原子性等写入前检查不得以“内部互信”为由删除。

文件身份、并发数据库状态、写入授权等会随时间或并发独立变化的事实，必须在副作用点重新观测；这是同步、授权或 TOCTOU 防护，不是跨层重复业务校验。格式或协议已有权威解析器时，以其为唯一判定来源，不得先维护一套自写“预检解析器”再交给权威解析器复验。

LLM 返回内容必须在响应进入内部译文模型时完成结构、对应关系以及源语言/目标语言要求的验收。验收通过后的译文在任务切分、持久化和写回模块之间流转时不得反复做相同校验。

首次接入 Lua 执行时必须明确脚本属于受信用户代码还是不受信输入；若不受信，必须先定义文件、网络、进程等能力边界和执行预算。校验 Lua 返回值不能替代执行权限控制。

## 7. 数据库保持最小且由用例驱动

数据库服务于初始化、提取、翻译和写回之间的持久化交接，不用于预演未来平台。设计或新增字段前，必须能回答：

- 当前哪个用例写入它；
- 当前哪个用例读取它；
- 它影响哪个用户可观察行为；
- 删除它后当前需求为何无法正确实现。

无法回答的字段、表、索引、状态、审计副本、缓存和扩展槽不得加入。可从规范事实推导的计数、摘要和缓存默认即时计算，只有测量证明必要时才持久化。不得因归档版本已有某字段而保留它。schema 应随已经实现的纵向切片小步演进；迁移只服务实际行为。

共同数据只表达跨引擎稳定的翻译事实。引擎专用定位和格式知识必须由相应引擎边界拥有，不能为了首个 MZ 实现把整个共同 schema 固化为 RM 模型。具体存储表达在用例接口确定前不预设。

对于当前用例明确需要持久化的已提取原文、定位、翻译任务和译文等工作流状态，数据库是唯一权威记录；游戏文件仍是外部内容事实源和写回目标，数据库不授予写入权限。执行写回前必须重新确认项目根范围、目标身份以及外部变化、覆盖或并发冲突。日志、进度事件和临时任务对象不得保存一份相互竞争的持久业务状态。

同一 schema、版本号、字段定义、错误码或其他不变量只能有一个 owner。需要派生表示时由 owner 生成，不得手写多份镜像常量或 DTO。数据库版本和迁移只在建立受信 session 时检查一次；session 内各 repository 不再反复枚举全 schema。

## 8. 实现与测试纪律

- 先用高层行为测试证明应用用例正确，再为真实边界实现增加契约测试，最后以最小真实 MZ fixture 做纵向集成验证。
- fake 只用于测试或本地验证装配，不得进入生产组合根和成功路径。
- 测试外部边界的接受与拒绝、内部转换和最终可观察结果；不得在每个内部模块复制同一组非法输入测试。
- 并发翻译必须有明确且真实生效的在途上限、任务块上限和失败语义，不得无界创建任务。只有当前用例需要时才加入重试、限流、取消、恢复或成本治理。
- 第三方库应封装在具体实现内部。只有当前接口实现确实需要时才增加依赖，禁止先囤积基础设施依赖。
- 新 workspace 使用 Rust 2024 edition 和显式 Cargo resolver；MSRV、固定工具链、feature 与发布配置必须依据真实目标另行确定，不得从归档继承。
- workspace 建立后，至少运行与改动相关的格式化、Clippy 和测试；项目命令确定后再把它们写入现行文档，不得运行归档工程的门禁来证明新项目正确。

## 9. 明确禁止重复的失败模式

- 禁止 Rust/Python 绑定式核心架构和两套领域模型。
- 禁止先完成数据库、IO、日志、LLM、Lua 等底层“大而全”模块，再开始真实 CLI 业务。
- 禁止把“接口先行”解释为预先设计所有底层公开 API；接口只能由当前上层用例推导。
- 禁止跨模块重复校验内部数据，或用不同校验规则造成 A 能接受而 B 拒绝。
- 禁止为未来猜测设计大量数据库字段、状态机、资源预算、配置旋钮和错误类别。
- 禁止让共同核心依赖 MZ/MV 专用数据模型，或要求其他引擎适配 RM 结构。
- 禁止万能磁盘 IO、万能数据库门面或其他没有真实上层调用方的基础设施工程。
- 禁止用局部完成、接口空壳、合成测试或归档代码宣称完整引擎流程已经可用。

## 10. 任务交付前检查

交付每个编程任务前，至少回答：

1. 本次完成了哪个用户可观察结果，哪些依赖仍只有接口？
2. 实现是否从最高层用例向下推进，还是偷偷回到了基础设施优先？
3. 新代码中有哪些逻辑具有现实复用性，已经抽取或为何暂不抽取？
4. 模块是否隐藏自身复杂性，只暴露高级、最小接口？
5. 每项外部数据在哪里完成唯一一次校验，内部是否还有重复校验？
6. 每个新增数据库字段是否有当前读写方和行为依据？
7. 引擎专用知识是否全部留在引擎边界？
8. 实际运行了哪些验证，哪些能力仍未实现或未验证？

# AGENTS.md

本文件是当前项目长期有效的方向基线与知识治理契约，用于约束产品形态、架构推导、开发决策和知识同步，确保项目在持续实现中仍然朝向同一个目标。

这里记录判断原则与知识分工，不记录当前命令、参数、模块名、数据字段、第三方库、完成状态或具体工作流。现行规格描述已经确认的外部契约，代码与测试描述已经实现的事实。

方向规则优先说明应如何判断和行动，以抽象原则统摄具体案例。对话中的具象例子用于识别背后的职责、不变量和取舍，不自动成为通用规格；长期基线不通过罗列历史禁令来代替正向设计标准。

> 总纲：从真实用户结果反推纵向切片，从当前职责推导直接依赖；允许跨越物理层级，但不绕过语义所有者；只设计当前目标及其直接依赖，以现实消费推动模块复用和数据模型演进。

## 1. 产品形态：统一入口，独立纵向切片

项目的产品目标是以一个 CLI 聚合多个游戏引擎的翻译能力，同时让不同引擎保持彼此独立的命令域和纵向切片。统一入口只承担真正跨引擎一致的进程级职责和路由；每个引擎完整拥有自己的命令、参数、行为、错误语义和演进节奏。

表面相同的命令或流程不自动构成同一契约。各引擎先准确表达自己的用户意图，再在契约之下复用语义一致的能力。

面向用户的入口只表达本次操作意图和确实需要当场选择的事实。已经由项目状态或执行配置建立的事实直接复用；经证据确认且确属部署环境、外部服务或操作者选择的长期策略由统一配置边界承担，固定产品语义、内部资源调优、调度细节和运行时观测由各自责任方拥有，不转嫁为每次操作都要重复填写的参数或伪装成用户配置。

```text
[统一产品入口]
       |
       +----> [引擎纵向切片 A] ----+
       +----> [引擎纵向切片 B] ----+----> [共享能力网络]
       +----> [引擎纵向切片 C] ----+

纵向切片拥有外部契约和编排权；
共享能力只承载已经确认的一致语义。
```

一个纵向切片从用户入口贯穿到可观察结果，包含完成该结果所需的业务判断、状态交接和副作用。项目进度以真实纵向结果衡量，不以目录、模块、crate 或底层组件的数量衡量。

## 2. 从结果出发，自上而下推进

每项任务先明确它要完成的结果，再决定代码结构。开始实现前至少回答：

1. 谁在什么场景下需要这个结果；
2. 成功、失败和必须保持的行为分别是什么；
3. 哪个当前目标对结果负最终责任；
4. 当前目标真正需要哪些直接能力；
5. 本次任务在哪里停止，如何证明已经完成。

对于内部模块任务，以已确认的上游契约和模块自身应交付的行为作为起点；技术组件本身不是目标。

```text
[用户结果 / 已确认的上游契约]
                 |
                 v
             [当前目标]
                 |
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]

========== 当前设计停止线 ==========
```

当前任务只展开目标及其全部直接依赖。到达直接依赖的公开契约后停止，不递归设计它们的内部依赖。某个依赖进入后续任务时，把它作为新的当前目标，重新从其上游结果推导职责和直接依赖。

端到端任务可以逐步扩大实现范围，直到真实结果成立；每次扩大仍遵循同一推导过程，避免一次性预造整棵依赖树。

## 3. 契约先于实现

先从消费方视角定义承诺，再选择实现形态。一个契约应当说明：

- 调用方提交什么意图；
- 提供方返回什么结果；
- 成功和失败分别意味着什么；
- 哪些复杂性由提供方完整承担；
- 哪些行为是调用方可以长期依赖的。

契约先行不等于 trait 先行，也不等于模块或 crate 先行。函数、类型、模块公开面或 trait 都可以承载契约；采用满足当前调用关系的最简单形式。只有确实存在替换、隔离、独立测试或多实现需求时，才引入相应的抽象机制。

接口由消费需求塑造，以职责语言表达高级意图。算法、存储、协议、第三方类型和资源治理留在实现边界内部。

跨进程、外部服务或扩展调用同样遵循最小职责信息原则：只提供对方完成当前任务所需的有效事实，内部表结构、持久化身份、调度状态和恢复映射继续留在本方边界。外部协议描述“对方要做什么”，不把本程序如何实现这件事泄漏为对方必须理解的知识。

## 4. 依赖由语义责任决定

项目依赖是能力网络，不是必须逐层经过的阶梯。物理目录、架构图高度和“底层”标签不决定能否直接依赖。

当前模块可以直接依赖任何自己确实需要的稳定能力，包括位于更底层的核心能力；同时由真正拥有业务规则、状态和不变量的模块承接相应语义。

```text
合理的直接依赖：

       +----> [业务能力 B]
[A] ---+----> [业务能力 C]
       +----> [A 自己需要的核心能力 D]

需要经过语义所有者：

[A] ----> [B：拥有规则与状态] ----> [底层机制 D]
```

判断一条直接依赖是否成立时，依次确认：

1. 当前模块是否对该能力存在独立需求；
2. 调用是否只依赖对方职责级的公开契约；
3. 是否绕过了其他模块拥有的规则、状态或不变量；
4. 候选中间层是否增加了语义转换、策略、保护或生命周期责任；
5. 依赖方向是否保持单向并且能够避免循环。

当中间层只改名或转发调用时，直接依赖更准确；当中间层拥有不可替代的业务含义时，通过它协作。组合根可以直接看见具体实现以完成构造、配置和生命周期装配，但业务执行仍通过相应契约完成。

外部入口和呈现层把业务执行交给相应责任模块，不反向成为其实现依赖。共享核心能力只承载已经确认的一致语义，不吸收特定调用方的规则；属于单一领域的底层能力可以保留该领域语义，并按现实需要被直接依赖。

## 5. 模块边界与复用演进

模块围绕单一、可命名的职责形成。它拥有与该职责相关的知识和变化原因，把内部状态、算法、第三方细节和复杂流程留在边界内，对外提供降低调用方认知成本的高级接口。

每次编写具体实现代码前，复用审计都是必经步骤，不只在新增游戏引擎时进行。新增纵向切片、实现直接依赖、补充根适配器、扩展既有模块或修复共享逻辑时，都先对照现有源码，分别作出以下判断：

1. 语义、规则、不变量、错误语义和生命周期一致的已有能力，直接复用；
2. 只有部分职责一致时，从既有实现中提炼最小、可命名且能独立验证的公共能力，由既有消费方和新消费方共同使用；
3. 仅代码形状相似，但业务语义、约束或演进方向不同时，明确记录差异并保持独立。

新增游戏引擎时，必须重新审计现有能力的语义、责任和现实消费者。语义完全一致的能力，应在同一次改动中提炼为唯一共享实现，并同步迁移全部现有消费者、配置、测试、规格和依赖图；语义不同的能力保持独立。新增引擎不得通过复制现有纵向切片、在共享模块中累积引擎判断分支、增加无语义转接层或平行实现来规避结构调整。只有全部受影响代码收敛到最终唯一结构后，新增引擎才算完成。

公共化不以“整个模块完全一致”为前提；既有模块中只要有局部职责能在不携带原调用方特有规则的前提下独立成立，就可以提炼为公共能力。提炼时同步迁移既有消费方、测试、现行规格和依赖图，不先复制出平行实现，也不保留新旧双轨。复用的目标是消除重复知识和重复机制，不是强迫不同业务经过同一流程，也不是追求形式上的公共层。

拆分必须产生可说明的工程意义，过度拆分与重复实现同样属于需要阻止的结构性问题。不得仅因文件较长、目录整齐、层级对称、代码形状可分或假想的未来复用而新增模块、接口或 crate。只有当拆分后的单元具有独立可命名的职责、自己的不变量或变化原因、现实消费方，并且能降低调用方认知成本，或形成必要的依赖、编译、发布或所有权隔离时，拆分才成立。共同维护同一不变量、总是一起变化、或离开彼此无法独立理解和使用的代码保持在同一模块或 crate。拆分不得制造无语义转发层、碎片化浏览、公开面膨胀、依赖爆炸或循环依赖；物理边界始终采用足以承载当前真实语义的最小形式。

在复用审计和实现过程中持续判断：

- 这段知识由谁负责；
- 它是否形成可以独立命名和测试的职责；
- 当前实现是否混入了不属于本职责的知识；
- 现在或可明确指出的后续消费方是否需要相同语义；
- 提炼后能否减少重复知识，而不只是增加调用层级。

发现复用可能时，先让代码具备清晰、窄小的职责边界；共享公开面随现实消费增长。现实消费既可以是已经存在的调用方，也可以是由已确认方向明确需要的后续调用方，不要求先产生第二份重复代码。复用依据是语义一致，而不是代码形状相似、函数名称相近或处理步骤看起来相同。

```text
[独立职责]
     |
     v
[切片内清晰边界]
     |  相同语义出现现实消费
     v
[共享能力模块]
     |  需要独立依赖、编译、测试或发布
     v
[独立 crate]
```

围绕同一职责、共享同一组内部实现并适合共同编译和发布的 `mod` 放在同一个 crate。只有依赖隔离、编译边界、发布边界或所有权边界确实独立时，才提升为新的 crate。

共享能力组成按需连接的网络。每个纵向切片只选择自身需要的节点并保留编排权；共享层不演变为要求所有业务经过同一流程的总管框架。

## 6. 简洁表达、数据类型与配置

代码和数据设计追求“用尽可能少的概念完整表达当前真实语义”。简洁不是机械减少文件、类型或代码行，而是减少调用方必须理解的无效概念。每个类型、字段、状态、接口和中间层都应能够指出自己的责任方、现实消费者以及它消除的复杂性；后续需求出现时通过受控演进扩展现行模型，不以预留概念代替当前设计。

进程内部优先使用语言原生、拥有明确所有权并能承载不变量的领域类型。互斥模式使用枚举表达，组合事实通过字段私有的值类型和构造边界建立，使非法状态尽量无法构造。通用文本树、字符串标签和松散键值停留在解析、序列化或持久化边界；进入内部后转换为受信类型。用于诊断的展示文本不反向成为权威身份或解析协议。

面向人类或模型执行者编写的外部格式也是产品接口。它以易写、易读、表达力强和适合大规模生成、审核为目标，只保留完成当前意图所需的字段。需要复杂表达能力时优先采用成熟、广泛理解的既有标准，并把项目约束收窄到自身真正消费的语义。业务模型、输入输出协议、持久化格式和配置不建立版本概念，不包含业务版本字段、版本枚举或版本分流，也不引入用于兼容、识别或迁移历史格式的旧字段别名或扩展层；元数据和扩展能力只有在当前实现存在现实消费时才进入契约。

面向 LLM 的 user message 是完成本次模型任务的最小载荷，不是内部领域模型、持久化结构或未来扩展协议的镜像。只发送模型产生当前输出不可缺少、且尚未由 system message 或既有上下文建立的事实；能够由顺序、现有分组或调用侧受信状态稳定推导的内容不得重复编码。默认沿用最短、直接、易读的现有表达；除非当前任务确实需要无歧义的嵌套或转义且现有表达无法承载，否则不得改造成 JSON，也不得为了格式统一、机器可读、调试便利或假想扩展增加包装层、标签、空字段、重复计数或重复约束。任何新增内容都必须能够指出当前模型的现实消费行为及其不可替代性。

外部松散字段只有在当前流程确实需要读取、写回或无损转发时才保留，并且只停留在相应边界。无损转发本身可以是现实消费，但不得把不透明字段扩散为业务模块必须理解的事实，也不得以未来消费者为由扩大内部模型。

项目实行严格的单版本实现原则。现行规格是系统唯一有效的版本定义，每次版本迭代都以新规范一次性替换旧规范；迭代完成后，生产代码、测试、测试夹具、配置、现行规格、代码注释、错误类型、诊断、日志和用户提示只描述并实现当前版本。不得以任何形式保留或重新引入旧版本，包括兼容、识别、检测、探测、专用拒绝、迁移、转换、回退、降级、警告、提示、条件分支、旧格式解析器、旧字段或旧值别名、双读双写以及仅为证明旧版本行为而存在的测试和样本。

所有输入只依据当前规范解析和校验。不符合当前规范的内容统一作为普通无效输入处理，使用当前契约已有的通用错误语义；系统不得判断、记录或提示该输入可能属于旧版本，也不得通过版本号、特征字段、格式指纹或失败后的备用解析路径推断其历史身份。旧版本数据和调用方不构成当前实现的运行时场景，不能据此增加代码路径。

版本切换必须在同一次改动中删除旧实现及其全部残留，并同步更新当前消费者、数据定义、测试、配置、现行规格和依赖图，不采用过渡期兼容层或分阶段保留。已有数据若不符合新规范，应在当前实现之外完成替换、重建或清理；不得在当前代码库中为其增加迁移器或专用识别逻辑。工具链要求的包版本或构建版本只能标识当前制品，不得参与业务分流、输入识别或历史版本枚举。

资源治理机制本身必须先被证明必要。只有真实的外部服务配额、操作系统或协议硬约束、已经测量的性能瓶颈、会导致现实故障的资源竞争，才能支持引入并发上限、队列、超时、批量、重试、等待窗口或容量拒绝。配置已经显式、已有消费者、参数可以调节或机制看似能够保护用户电脑，都不能证明该机制合理；不得先造治理机制，再要求配置为它赋值。

机制成立后，再按事实所有权决定其取值。部署环境、外部服务或操作者确实能够选择的策略进入统一配置边界；固定领域事实和产品语义由代码契约承载；内部 worker、调度窗口、传输批次、重排窗口、连接复用和缓存策略由实际执行者根据基准与运行时事实拥有。影响资源占用不等于必须外部配置，内部实现细节不得伪装成用户业务选择。缺失必填外部选择时显式失败，没有真实选择的模块不创建空配置对象。

项目规模不是可配置策略，也不接受 ATT 自行规定的产品上限。不得对文件字节数、目录项数、目录深度、目录总字节、Lua 值大小或复杂度、SQLite 查询组数、行数或结果字节，以及 Claim、Unit、Group、Task 等业务总量建立人为上限、拒绝分支或对应配置。“无上限”意味着不存在这项比较和拒绝逻辑，不是把默认值换成一个很大的常量。输入只能因真实的文件系统、操作系统、地址空间、内存、SQLite、外部协议或数据格式错误而失败；程序不得为了替操作者预防资源耗尽而提前拒绝规范内的输入，并且必须完整报告实际失败原因。

命名、类型和注释共同表达领域责任。命名优先回答“这是什么职责或事实”，注释重点解释业务含义、不变量、来源映射和非显然取舍，不重复代码表面动作，也不用臆造的语义名称覆盖需要保留的原始事实。

## 7. 语义所有权、信任与错误

每项业务事实、规则和不变量都有唯一责任方。外部输入由最了解其语义的边界完成解析、校验和可信转换；一旦转换成功，内部模块按契约直接协作，不重复建立或重新解释同一事实。

```text
[外部事实]
     |
     v
[责任边界：解析、校验、转换]
     |
     v
[承载不变量的内部数据]
     |
     +----> [内部模块] ----> [内部模块]
```

内部信任是对契约和已建立不变量的信任，不是忽略现实失败。静态业务语义在所属边界建立一次；会随时间变化的外部状态，由真正执行副作用的模块在操作发生前重新观测。输入错误、运行环境失败和内部不变量破坏保持不同含义。

错误由责任边界转换成调用方能够理解和处理的语义，同时保留定位问题所需的完整原因与上下文。具体错误在仍持有类型、阶段、路径、字段、数值、稳定错误码和提交终态时建立安全结构化投影；CLI、日志和其他输出只消费这份投影，不通过顶层 downcast、解析 `Display` 或遍历任意错误对象补猜事实。

错误和日志必须清晰明示具体失败内容及安全的底层原因。输出至少应在事实存在时展示命令与阶段、路径或对象、实际值与期望值、OS/SQLite/HTTP 等稳定代码、经过清理的底层原因、状态是否改变、处理办法和恢复位置；责任域分类只能作为补充，禁止用“输入错误”“项目不可用”“运行失败”“内部错误”等泛化类别替代具体诊断。主错误、相关错误和清理错误不得互相覆盖或被队列压力丢弃。

只隐藏凭据、授权 Header 值、Prompt、模型正文以及明确判定为敏感的用户内容；隐藏敏感值不得连带抹去安全的阶段、路径、字段名、状态码、SQLite 或 OS 代码、实际值与上限、事务和发布终态。任何为了防泄密而吞掉可安全公开详情的实现都属于错误设计。

业务允许的未产出、部分结果和暂时不可用属于正常结果，不因结果不完整而升级为程序错误；只有模块已经无法继续履行契约或保持状态一致性时才返回错误。正常结果仍应携带足够的结构化诊断事实，并由明确的可观测性边界统一记录，避免各业务模块自行拼接和分散持久化日志。

可观测性产物默认是业务行为的非权威旁路投影，不参与业务状态判断、提交、恢复、重放或副作用准入。普通可观测性自身失败应形成安全且可见的诊断，但不改变原业务结果、项目状态或退出码。只有已确认的用户结果、产品契约或外部约束明确把证据完整性作为操作准入、成功或一致性条件时，记录产物才属于业务状态或审计账本；此时必须由现行规格明确其现实消费方、权威性、持久化关系和失败语义。记录内容的重要性、敏感性、完整性或保留要求本身不能证明业务门禁成立。

开放扩展能力先明确自己的信任模型和协议所有者。扩展拥有自身数据协议、生命周期、事务和跨阶段交接时，核心向它提供约定的宿主能力和受信上下文，不主动猜测、迁移或消费其不透明产物；扩展显式采用核心契约时，双方才共享相应语义。统一产品入口不以削弱扩展表达能力为代价强迫所有数据进入同一标准流程。

## 8. 状态与持久化来自真实交接

状态模型从真实纵向行为之间的交接产生。只有后续行为确实需要跨时间、跨进程或跨阶段保留的事实才进入持久化。

领域上下文范围、昂贵工作的分配单位、结果验收单位和持久化原子单位分别由各自不变量决定，不要求彼此相同。相关事实可以作为整体提供上下文，能够独立确认的结果按最小有效单位验收，必须共同一致的传播或状态关系则在同一原子边界提交。批量工作不自动意味着整批成败；已经确认且保持一致的有效进展及时保存，未完成部分由后续运行根据权威状态重新推导。

批处理和流水线必须由真实领域关系、外部协议容量、已经测量的固定成本或吞吐瓶颈证明。语义原子保持完整，相关上下文尽量保留；容量利用可以摊薄已经证实的固定成本，但不通过拼接无关内容或无依据地切碎相关内容来追求表面满载。

项目总工作量与同时在途工作量必须严格区分。Claim、Unit、Group、Task、文档和 SQLite 行的总量不因内部窗口而受限；有界流水线只约束正在读取、计算、等待提交或执行外部请求的工作，超出窗口的工作自然背压而不是失败。内存可以主动用于索引、缓存、重排和保存完成结果以换取吞吐；没有换页、内存耗尽或代表性负载证据时，不得为了追求最低峰值内存引入磁盘 spool、逐项 actor 往返或全路径串行。并发可以改变完成时间，不能改变自然顺序、代表选择、分组结果或提交顺序。

```text
[当前行为]
     |
     | 后续行为必须继续使用的事实
     v
[持久化交接]
     |
     v
[后续行为]
```

每项持久化事实都应能够回答：

1. 谁负责写入；
2. 谁负责读取；
3. 它支持什么可观察行为；
4. 哪个位置是权威来源；
5. 它何时创建、更新和失效。

能够从权威事实稳定推导的数据在使用时计算。共享存储可以承载其他领域提供的信息，但解释权仍属于该领域。数据结构随已经确认的纵向切片小步演进，以真实读写关系证明每个字段存在的价值。

持久化结构优先直接表达领域资产及其真实读取、更新和事务边界。共同构成语义整体并经常一起消费的事实保持可见关联；需要独立继承、校验和写回的叶子保留精确身份。只有出现真实的跨领域统一读写需求时，才引入通用中间模型，避免以更通用但表达力更弱的结构替代清晰的领域关系。

## 9. 性能、并发与资源治理

性能是核心产品契约和完成条件，不是正确性之后可有可无的优化。选择 Rust 的现实价值必须体现在大型项目上的低墙钟时间、有效多核利用、低重复工作和可预测的原生执行；“使用了 Rust”“使用了异步或 Rayon”“增加了线程”都不能替代实测结果。不是每次局部修改都必须运行大型性能测试；但改动规模较大，或根据算法复杂度、数据流、并发、存储、日志、序列化、文件处理和外部调用方式可以合理判断其可能影响性能时，必须与冻结基线比较。未经证据不得删除既有并行、增加串行屏障、重复扫描、重复解析、磁盘中转、逐项事务、逐项耐久刷新或队头阻塞。

需要性能验证时，项目以测试集中规模与结构综合最复杂、最大的真实游戏作为主要验收样本，并在被 Git 忽略的 `tmp/` 下建立独立测试工作区、测量脚本、原始记录和报告。使用 Release/MSVC 实际制品、全新的来源副本、项目工作区、数据库和输出目录完整执行，禁止复用前一轮 Extract、译文、WriteBack 结果，禁止通过 unchanged 快速路径、预热的应用缓存、减少提取覆盖、跳过校验、降低持久化或发布语义来取得成绩；操作系统缓存必须受控并在报告中说明。运行内由现行语义要求的全局去重不属于缓存取巧。

在冻结的基准机器和上述最大真实游戏上，三项核心路径的目标分别为：Extract 尽量压缩到 10 秒以内；Translate 排除模型供应商实际生成与网络等待时间后，将项目读取、预处理、全局去重、任务构造、响应验收、传播和持久化压缩到 10 秒以内；WriteBack 将完整资产读取、布局、文档改写、候选构建、Lua、验证和单次发布压缩到 10 秒以内。Translate 使用立即返回确定性合法结果的本地 Provider 测量 ATT 自身时间，但不得跳过任何真实任务和提交工作。任一阶段尚未进入 10 秒时，不得把性能工作宣称为完成，必须保留分阶段测量、瓶颈证据和继续优化的明确缺口。

性能不得用逻辑错误、行为删减或核心能力退化交换。提取覆盖、自然顺序、指纹、全局去重、代表与 seed 选择、冲突检测、任务内容和编号、事务原子性、部分翻译进度、Rules、Lua、取消、候选清理、完整验证和单次目录发布均为硬门禁；输出或状态存在任何逻辑差异时，性能成绩无效。发现核心路径中的超线性算法、重复工作、错误状态或不一致行为必须修复，不得作为已知问题带着交付。

并发方式根据工作负载选择：等待型 I/O 使用可背压的异步或专用阻塞执行资源，CPU 密集型工作使用受控的数据并行；独立文档、Group、规则、任务和响应默认允许并行或与相邻阶段重叠，只有明确的数据依赖、确定性归并、单写者事务、单 VM 或最终发布边界可以保持串行。串行边界必须尽量短，并能说明不可并行的语义依据；以“保持顺序”为由串行全部计算属于设计错误。

异步和多线程设计必须明确真实并发关系、背压、取消、错误传播和任务生命周期。内部 worker、窗口和批次通过最大真实样本的消融实验选择吞吐平台点，由代码或运行时观测拥有；只有供应商并发、RPM、网络超时等真实外部约束进入配置。并发不得破坏结果确定性、持久化一致性和错误语义。

性能优化先用分阶段计时、CPU 与磁盘利用率、事务和同步次数、算法复杂度及必要的 profiler 证明瓶颈，再针对瓶颈实施。验证至少记录端到端墙钟时间、各阶段耗时、有效 CPU 核数、吞吐、磁盘读写与同步、事务次数和峰值内存，并进行多次交错运行；只展示单次最好成绩、只测缓存命中或无法量化收益的复杂化不算完成。不得交付相对冻结基线出现严重性能下滑的核心路径。若某项回退确实不可避免，必须给出分阶段测量和 profiler 证据，说明新增成本对应的必要语义或外部硬约束、已经尝试过哪些优化，以及为什么在不破坏正确性和产品契约的前提下无法继续消除；“实现更简单”“改动更统一”或未经验证的推测不能作为解释。真实测试集和 `tmp/` 中不纳入版本控制的测量脚本可以按当前风险直接调整，不为历史材料建立兼容层。

## 10. 实现、验证与完成定义

当前版本的用户可观察契约、持久数据定义和现实外部消费者决定本次改动必须同步更新的范围，但不产生对旧版本的兼容责任。版本迭代一经确认，旧契约、旧数据格式和旧消费者即退出现行范围，不得因其曾经存在而保留兼容、识别、拒绝、迁移或提示逻辑。既有结构无法准确表达当前语义、制造重复知识或持续阻碍实现时，采用范围明确的破坏性重构，并在同一次改动中更新全部当前消费者、测试、配置、现行规格和依赖图，彻底删除旧实现，不以转接层、双轨模型或历史专用错误保存已经失效的设计。

依赖树继续向下推进时，所有仍包含领域判断、状态转换或编排规则的非根能力都应获得真实实现；最终根接口只承载文件、计算、存储、网络、时间和运行时等环境机制。组合根统一负责外部配置转换、具体实现构造和生命周期管理。非根业务算法成立、根接口已经定义、根适配器真实可用和生产装配贯通是不同完成层级，交付时分别说明。

实现顺序与设计顺序保持一致：

1. 用高层行为固定当前目标的可观察结果；
2. 从消费方需要定义直接依赖的最小契约；
3. 实现并验证当前目标，范围外依赖使用测试替身；
4. 某个依赖进入实现范围后，再以其上游契约为起点实现并验证；
5. 端到端范围要求成立时，用最小真实样本贯通纵向路径；
6. 共享能力出现多个消费方后，验证各方依赖的是同一语义。

依赖暂未实现时，可以使用测试替身验证当前目标；生产装配只连接已经具备真实实现并完成相应验证的能力。契约存在、局部测试通过和底层组件完成都不单独代表纵向切片已经完成。

当前任务完成，意味着约定范围内的结果或上游承诺已经成立，失败语义明确，并完成相应验证。完整纵向切片完成，则进一步要求用户结果在真实装配下成立，必要状态能够正确交接，相关副作用和资源边界已经验证。未实现、未接线和未验证的范围在代码、文档和交付说明中保持可见。

架构图、现行规格和测试随已落地事实同步更新。图只描述真实依赖或明确标注的候选设计，不把设想呈现为现状。

用户入口、状态判断、产品契约、调查与决策路径、完成证据或恢复方式发生变化时，必须在同一次改动中更新相应的语义所有者和现实消费者：现行规格和测试随契约变化同步，指南随调查、选择或验证方法变化同步，Skill 随触发条件、状态路由、必读材料、任务组织或停止与回退判断变化同步。存在已知冲突、失效链接或过期路由时，任务尚未完成。只有长期方向、架构判断方法、项目边界或知识治理规则发生变化时才更新本文件。

## 11. 每次任务的方向检查

设计、实现和评审都使用以下问题校正方向：

1. 当前工作最终服务什么用户结果或上游承诺；
2. 谁拥有该结果及其中的规则和不变量；
3. 当前目标的直接依赖分别是什么，为什么是直接依赖；
4. 是否存在无语义增值的转发层，或绕过语义所有者的调用；
5. 新模块抽取的是稳定职责还是未经证明的未来设想；
6. 编写具体实现前是否已审计现有完整能力和局部职责，并说明哪些直接复用、哪些提炼共享、哪些因语义不同而保持独立；
7. 候选拆分是否具有独立职责和现实收益，还是只让文件更短、目录更整齐或层级更对称；拆分后是否反而产生转发层、碎片化、公开面或依赖膨胀；
8. 外部事实在哪个边界成为受信内部数据；
9. 新增状态是否有明确的写入方、读取方和行为用途；
10. 哪些证据能够证明纵向结果，而不只是证明局部代码可运行；
11. 每个并发上限、队列、超时、批量、重试、等待窗口、容量拒绝或磁盘 spool，分别由什么外部硬约束、真实故障或测量证据证明；是否误把配置显式、存在消费者或保护用户电脑当成必要性；
12. 内部模型是否用强类型直接表达不变量，外部格式是否保持最小且易于大规模书写；
13. 已经证明必要的策略，其值为什么由外部配置、代码契约或运行时观测拥有；是否把内部 worker、队列、批次和缓存错误地转嫁给用户配置；
14. 是否对项目总量建立了任何 ATT 自行规定的文件、目录、Lua、SQLite、Claim、Unit、Group 或 Task 上限；所谓无上限是否真正删除了比较和拒绝路径；
15. 各输出是否明示安全的底层原因、阶段、路径、稳定错误码和终态；是否用泛化类别或防泄密理由吞掉了可公开详情；
16. 新增可观测性产物是否仍是非权威旁路；若其失败会改变业务结果或项目状态，什么现实消费方和已确认契约证明它属于业务状态或审计账本；
17. 批处理、流水线、验收和提交粒度是否分别符合领域关联、实测成本与一致性要求；在途窗口是否只形成背压而不限制总工作量；
18. 改动规模或性质是否有合理可能影响性能；若有，是否依据测试集最大最复杂真实游戏，在 `tmp/` 的全新状态下完成无缓存捷径的 Release 基准，并持续逼近或达到 Extract、Translate、WriteBack 各自 10 秒目标；
19. 性能结果是否保持全部核心语义且没有严重回退；不可避免的回退是否有测量、剖析、必要语义和已尝试优化的完整证据，而不是通过减少工作、降低保证或主观解释掩盖；
20. 新增领域事实是否已经由真实样本、权威说明或实际消费验证；
21. 版本迭代后是否已经从生产代码、测试、夹具、配置、现行规格、注释、错误和诊断中删除旧版本的全部痕迹，并确保不符合当前规范的输入只按普通无效输入处理。
22. 当前知识是否只写在唯一语义所有者中，相关规格、指南、Skill 与验证是否已经同步；Skill 是否只编排并按需读取现行事实，而没有复制契约或绑定特定执行平台。

这些问题尚不能回答时，先收束目标和边界，再扩大实现范围。任何技术选择都应能够沿依赖链回溯到真实需求。

## 12. 可执行知识系统、事实源与历史材料

ATT 将项目知识组织为能够从目标、状态、权威事实、行动和证据闭环的可执行系统，而不是把全部知识复制到单一入口。每项事实只允许一个语义所有者；其他材料通过链接或明确引用消费它。发现知识缺口时，先在责任边界补充并验证事实，再让其他入口使用，不把缺失契约藏进编排层。

知识系统按职责分工：

- 本文件保存长期产品方向、架构判断方法、知识治理和同步门禁；
- 现行规格保存已经确认的产品与外部接口契约；
- 指南解释如何调查事实、选择机制和组织验证，不重新定义规格；
- 代码和测试保存已经实现的行为与验证证据，不能反向覆盖现行规格；
- 代表性真实材料、权威说明、实际读写关系和验证记录证明领域事实与能力是否成立；
- Skill 保存触发条件、状态路由、任务组织和按需阅读规则，是执行入口与编排层，不是新的产品事实源。

文档必须能够独立阅读和审查，不依赖某个 Skill 才能成立。Skill 从当前目标和已有状态选择纵向切片，只读取当前任务所需的现行文档，建立覆盖当前目标及直接依赖的短任务清单，并依据可观察证据决定继续、停止或返回事实来源。除触发与导航需要的最小稳定摘要外，Skill 不复制命令、参数、schema、字段矩阵、错误语义、完整协议、代码示例或详细领域规则，也不以固定阶段顺序取代真实状态判断。

面向执行者的知识不绑定具体模型产品或执行平台，不规定主从角色、委派拓扑、固定报告协议或持久任务文件。需要指代参与方时，使用“使用者”“执行者”或“协作者”等与平台无关的职责语言；是否引入协作者由当前任务和可用能力决定。

统一知识入口只提供导航和编排，不把不同引擎表面相似的流程提升为共同契约；各引擎的语义、判断和演进仍由相应纵向切片拥有。

领域模型、自然顺序、关联范围和边界行为以代表性真实材料、权威说明和实际读写关系共同验证。示例和测试夹具用于解释已经确认的事实，不能凭空创造字段、关系或命名再反向塑造领域模型。需要定位、继承或写回的原始字段、精确来源和未经授权的原值保持可追溯；语义化类型、注释和高层视图用于补充理解，不覆盖权威事实。

版本控制历史是当前系统旧版本材料唯一允许存在的位置；当前工作树的任何目录，包括 `archive/`，都不得保留当前系统的旧实现、旧协议、旧格式、旧配置、旧测试或兼容方式，也不得引用、加载、编译、识别或验证这些内容。被替换或退役的 Skill 与工作流同样只属于版本控制历史，不作为现行知识源、模板、安装入口或兼容对象。`archive/` 只保存与当前系统版本无关的领域原始材料，用于验证当前领域事实和识别失败模式，不得成为当前系统旧版本的存放处。历史中仍然适用于当前实现的知识必须依据当前事实重新验证，并在现行责任边界重新表达后才能进入当前项目。

当前参数、临时命名、实现进度和候选细节进入对应的现行规格或代码文档，不进入本文件。

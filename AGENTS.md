# AGENTS.md

本文件是当前项目长期有效的方向基线，用于约束产品形态、架构推导和开发决策，确保项目在持续实现中仍然朝向同一个目标。

这里记录判断原则，不记录当前命令、参数、模块名、数据字段、第三方库或完成状态。现行规格描述已经确认的外部契约，代码与测试描述已经实现的事实。

方向规则优先说明应如何判断和行动，以抽象原则统摄具体案例。对话中的具象例子用于识别背后的职责、不变量和取舍，不自动成为通用规格；长期基线不通过罗列历史禁令来代替正向设计标准。

> 总纲：从真实用户结果反推纵向切片，从当前职责推导直接依赖；允许跨越物理层级，但不绕过语义所有者；只设计当前目标及其直接依赖，以现实消费推动模块复用和数据模型演进。

## 1. 产品形态：统一入口，独立纵向切片

项目的产品目标是以一个 CLI 聚合多个游戏引擎的翻译能力，同时让不同引擎保持彼此独立的命令域和纵向切片。统一入口只承担真正跨引擎一致的进程级职责和路由；每个引擎完整拥有自己的命令、参数、行为、错误语义和演进节奏。

表面相同的命令或流程不自动构成同一契约。各引擎先准确表达自己的用户意图，再在契约之下复用语义一致的能力。

面向用户的入口只表达本次操作意图和确实需要当场选择的事实。已经由项目状态或执行配置建立的事实直接复用；长期策略、资源调优和内部实现选择由统一配置边界承担，不转嫁为每次操作都要重复填写的参数。

```text
[统一产品入口]
       |
       +----> [引擎纵向切片 A] ----+
       +----> [引擎纵向切片 B] ----+----> [共享能力网络]
       +----> [引擎纵向切片 C] ----+

纵向切片拥有外部契约和编排权；
共享能力只承载已经确认的一致语义。
```

一个纵向切片从用户入口贯穿到可观察结果，包含完成该结果所需的业务判断、状态交接和副作用。项目进度以真实纵向结果衡量，不以目录、模块、crate 或底层组件的数量衡量。

## 2. 从结果出发，自上而下推进

每项任务先明确它要完成的结果，再决定代码结构。开始实现前至少回答：

1. 谁在什么场景下需要这个结果；
2. 成功、失败和必须保持的行为分别是什么；
3. 哪个当前目标对结果负最终责任；
4. 当前目标真正需要哪些直接能力；
5. 本次任务在哪里停止，如何证明已经完成。

对于内部模块任务，以已确认的上游契约和模块自身应交付的行为作为起点；技术组件本身不是目标。

```text
[用户结果 / 已确认的上游契约]
                 |
                 v
             [当前目标]
                 |
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]
                 +----> [直接依赖契约]

========== 当前设计停止线 ==========
```

当前任务只展开目标及其全部直接依赖。到达直接依赖的公开契约后停止，不递归设计它们的内部依赖。某个依赖进入后续任务时，把它作为新的当前目标，重新从其上游结果推导职责和直接依赖。

端到端任务可以逐步扩大实现范围，直到真实结果成立；每次扩大仍遵循同一推导过程，避免一次性预造整棵依赖树。

## 3. 契约先于实现

先从消费方视角定义承诺，再选择实现形态。一个契约应当说明：

- 调用方提交什么意图；
- 提供方返回什么结果；
- 成功和失败分别意味着什么；
- 哪些复杂性由提供方完整承担；
- 哪些行为是调用方可以长期依赖的。

契约先行不等于 trait 先行，也不等于模块或 crate 先行。函数、类型、模块公开面或 trait 都可以承载契约；采用满足当前调用关系的最简单形式。只有确实存在替换、隔离、独立测试或多实现需求时，才引入相应的抽象机制。

接口由消费需求塑造，以职责语言表达高级意图。算法、存储、协议、第三方类型和资源治理留在实现边界内部。

跨进程、外部服务或扩展调用同样遵循最小职责信息原则：只提供对方完成当前任务所需的有效事实，内部表结构、持久化身份、调度状态和恢复映射继续留在本方边界。外部协议描述“对方要做什么”，不把本程序如何实现这件事泄漏为对方必须理解的知识。

## 4. 依赖由语义责任决定

项目依赖是能力网络，不是必须逐层经过的阶梯。物理目录、架构图高度和“底层”标签不决定能否直接依赖。

当前模块可以直接依赖任何自己确实需要的稳定能力，包括位于更底层的核心能力；同时由真正拥有业务规则、状态和不变量的模块承接相应语义。

```text
合理的直接依赖：

       +----> [业务能力 B]
[A] ---+----> [业务能力 C]
       +----> [A 自己需要的核心能力 D]

需要经过语义所有者：

[A] ----> [B：拥有规则与状态] ----> [底层机制 D]
```

判断一条直接依赖是否成立时，依次确认：

1. 当前模块是否对该能力存在独立需求；
2. 调用是否只依赖对方职责级的公开契约；
3. 是否绕过了其他模块拥有的规则、状态或不变量；
4. 候选中间层是否增加了语义转换、策略、保护或生命周期责任；
5. 依赖方向是否保持单向并且能够避免循环。

当中间层只改名或转发调用时，直接依赖更准确；当中间层拥有不可替代的业务含义时，通过它协作。组合根可以直接看见具体实现以完成构造、配置和生命周期装配，但业务执行仍通过相应契约完成。

外部入口和呈现层把业务执行交给相应责任模块，不反向成为其实现依赖。共享核心能力只承载已经确认的一致语义，不吸收特定调用方的规则；属于单一领域的底层能力可以保留该领域语义，并按现实需要被直接依赖。

## 5. 模块边界与复用演进

模块围绕单一、可命名的职责形成。它拥有与该职责相关的知识和变化原因，把内部状态、算法、第三方细节和复杂流程留在边界内，对外提供降低调用方认知成本的高级接口。

每次编写具体实现代码前，复用审计都是必经步骤，不只在新增游戏引擎时进行。新增纵向切片、实现直接依赖、补充根适配器、扩展既有模块或修复共享逻辑时，都先对照现有源码，分别作出以下判断：

1. 语义、规则、不变量、错误语义和生命周期一致的已有能力，直接复用；
2. 只有部分职责一致时，从既有实现中提炼最小、可命名且能独立验证的公共能力，由既有消费方和新消费方共同使用；
3. 仅代码形状相似，但业务语义、约束或演进方向不同时，明确记录差异并保持独立。

新增游戏引擎时，必须重新审计现有能力的语义、责任和现实消费者。语义完全一致的能力，应在同一次改动中提炼为唯一共享实现，并同步迁移全部现有消费者、配置、测试、规格和依赖图；语义不同的能力保持独立。新增引擎不得通过复制现有纵向切片、在共享模块中累积引擎判断分支、增加无语义转接层或平行实现来规避结构调整。只有全部受影响代码收敛到最终唯一结构后，新增引擎才算完成。

公共化不以“整个模块完全一致”为前提；既有模块中只要有局部职责能在不携带原调用方特有规则的前提下独立成立，就可以提炼为公共能力。提炼时同步迁移既有消费方、测试、现行规格和依赖图，不先复制出平行实现，也不保留新旧双轨。复用的目标是消除重复知识和重复机制，不是强迫不同业务经过同一流程，也不是追求形式上的公共层。

拆分必须产生可说明的工程意义，过度拆分与重复实现同样属于需要阻止的结构性问题。不得仅因文件较长、目录整齐、层级对称、代码形状可分或假想的未来复用而新增模块、接口或 crate。只有当拆分后的单元具有独立可命名的职责、自己的不变量或变化原因、现实消费方，并且能降低调用方认知成本，或形成必要的依赖、编译、发布或所有权隔离时，拆分才成立。共同维护同一不变量、总是一起变化、或离开彼此无法独立理解和使用的代码保持在同一模块或 crate。拆分不得制造无语义转发层、碎片化浏览、公开面膨胀、依赖爆炸或循环依赖；物理边界始终采用足以承载当前真实语义的最小形式。

在复用审计和实现过程中持续判断：

- 这段知识由谁负责；
- 它是否形成可以独立命名和测试的职责；
- 当前实现是否混入了不属于本职责的知识；
- 现在或可明确指出的后续消费方是否需要相同语义；
- 提炼后能否减少重复知识，而不只是增加调用层级。

发现复用可能时，先让代码具备清晰、窄小的职责边界；共享公开面随现实消费增长。现实消费既可以是已经存在的调用方，也可以是由已确认方向明确需要的后续调用方，不要求先产生第二份重复代码。复用依据是语义一致，而不是代码形状相似、函数名称相近或处理步骤看起来相同。

```text
[独立职责]
     |
     v
[切片内清晰边界]
     |  相同语义出现现实消费
     v
[共享能力模块]
     |  需要独立依赖、编译、测试或发布
     v
[独立 crate]
```

围绕同一职责、共享同一组内部实现并适合共同编译和发布的 `mod` 放在同一个 crate。只有依赖隔离、编译边界、发布边界或所有权边界确实独立时，才提升为新的 crate。

共享能力组成按需连接的网络。每个纵向切片只选择自身需要的节点并保留编排权；共享层不演变为要求所有业务经过同一流程的总管框架。

## 6. 简洁表达、数据类型与配置

代码和数据设计追求“用尽可能少的概念完整表达当前真实语义”。简洁不是机械减少文件、类型或代码行，而是减少调用方必须理解的无效概念。每个类型、字段、状态、接口和中间层都应能够指出自己的责任方、现实消费者以及它消除的复杂性；后续需求出现时通过受控演进扩展现行模型，不以预留概念代替当前设计。

进程内部优先使用语言原生、拥有明确所有权并能承载不变量的领域类型。互斥模式使用枚举表达，组合事实通过字段私有的值类型和构造边界建立，使非法状态尽量无法构造。通用文本树、字符串标签和松散键值停留在解析、序列化或持久化边界；进入内部后转换为受信类型。用于诊断的展示文本不反向成为权威身份或解析协议。

面向人类或 Agent 编写的外部格式也是产品接口。它以易写、易读、表达力强和适合大规模生成、审核为目标，只保留完成当前意图所需的字段。需要复杂表达能力时优先采用成熟、广泛理解的既有标准，并把项目约束收窄到自身真正消费的语义。业务模型、输入输出协议、持久化格式和配置不建立版本概念，不包含业务版本字段、版本枚举或版本分流，也不引入用于兼容、识别或迁移历史格式的旧字段别名或扩展层；元数据和扩展能力只有在当前实现存在现实消费时才进入契约。

面向 LLM 的 user message 是完成本次模型任务的最小载荷，不是内部领域模型、持久化结构或未来扩展协议的镜像。只发送模型产生当前输出不可缺少、且尚未由 system message 或既有上下文建立的事实；能够由顺序、现有分组或调用侧受信状态稳定推导的内容不得重复编码。默认沿用最短、直接、易读的现有表达；除非当前任务确实需要无歧义的嵌套或转义且现有表达无法承载，否则不得改造成 JSON，也不得为了格式统一、机器可读、调试便利或假想扩展增加包装层、标签、空字段、重复计数或重复约束。任何新增内容都必须能够指出当前模型的现实消费行为及其不可替代性。

外部松散字段只有在当前流程确实需要读取、写回或无损转发时才保留，并且只停留在相应边界。无损转发本身可以是现实消费，但不得把不透明字段扩散为业务模块必须理解的事实，也不得以未来消费者为由扩大内部模型。

项目实行严格的单版本实现原则。现行规格是系统唯一有效的版本定义，每次版本迭代都以新规范一次性替换旧规范；迭代完成后，生产代码、测试、测试夹具、配置、现行规格、代码注释、错误类型、诊断、日志和用户提示只描述并实现当前版本。不得以任何形式保留或重新引入旧版本，包括兼容、识别、检测、探测、专用拒绝、迁移、转换、回退、降级、警告、提示、条件分支、旧格式解析器、旧字段或旧值别名、双读双写以及仅为证明旧版本行为而存在的测试和样本。

所有输入只依据当前规范解析和校验。不符合当前规范的内容统一作为普通无效输入处理，使用当前契约已有的通用错误语义；系统不得判断、记录或提示该输入可能属于旧版本，也不得通过版本号、特征字段、格式指纹或失败后的备用解析路径推断其历史身份。旧版本数据和调用方不构成当前实现的运行时场景，不能据此增加代码路径。

版本切换必须在同一次改动中删除旧实现及其全部残留，并同步更新当前消费者、数据定义、测试、配置、现行规格和依赖图，不采用过渡期兼容层或分阶段保留。已有数据若不符合新规范，应在当前实现之外完成替换、重建或清理；不得在当前代码库中为其增加迁移器或专用识别逻辑。工具链要求的包版本或构建版本只能标识当前制品，不得参与业务分流、输入识别或历史版本枚举。

所有会影响资源占用、性能、容量、等待、重试、持久化策略或外部服务行为的有意义选择，都由外部配置边界显式建立为受信类型，并通过构造参数注入真实消费者。统一配置边界负责读取原始来源、完成校验并决定缺失项；业务模块只接受类型化配置并准确执行，不再根据硬件、输入规模或库默认值另行推断策略。缺失必填选择时显式失败。固定的领域事实和产品语义由代码契约承载，没有真实配置选择的模块不创建空配置对象。

命名、类型和注释共同表达领域责任。命名优先回答“这是什么职责或事实”，注释重点解释业务含义、不变量、来源映射和非显然取舍，不重复代码表面动作，也不用臆造的语义名称覆盖需要保留的原始事实。

## 7. 语义所有权、信任与错误

每项业务事实、规则和不变量都有唯一责任方。外部输入由最了解其语义的边界完成解析、校验和可信转换；一旦转换成功，内部模块按契约直接协作，不重复建立或重新解释同一事实。

```text
[外部事实]
     |
     v
[责任边界：解析、校验、转换]
     |
     v
[承载不变量的内部数据]
     |
     +----> [内部模块] ----> [内部模块]
```

内部信任是对契约和已建立不变量的信任，不是忽略现实失败。静态业务语义在所属边界建立一次；会随时间变化的外部状态，由真正执行副作用的模块在操作发生前重新观测。输入错误、运行环境失败和内部不变量破坏保持不同含义。

错误由责任边界转换成调用方能够理解和处理的语义，同时保留定位问题所需的原因与上下文。模块不通过重复校验、解析其他模块的错误文本或建立第二套事实来获得安全感。

业务允许的未产出、部分结果和暂时不可用属于正常结果，不因结果不完整而升级为程序错误；只有模块已经无法继续履行契约或保持状态一致性时才返回错误。正常结果仍应携带足够的结构化诊断事实，并由明确的可观测性边界统一记录，避免各业务模块自行拼接和分散持久化日志。

开放扩展能力先明确自己的信任模型和协议所有者。扩展拥有自身数据协议、生命周期、事务和跨阶段交接时，核心向它提供约定的宿主能力和受信上下文，不主动猜测、迁移或消费其不透明产物；扩展显式采用核心契约时，双方才共享相应语义。统一产品入口不以削弱扩展表达能力为代价强迫所有数据进入同一标准流程。

## 8. 状态与持久化来自真实交接

状态模型从真实纵向行为之间的交接产生。只有后续行为确实需要跨时间、跨进程或跨阶段保留的事实才进入持久化。

领域上下文范围、昂贵工作的分配单位、结果验收单位和持久化原子单位分别由各自不变量决定，不要求彼此相同。相关事实可以作为整体提供上下文，能够独立确认的结果按最小有效单位验收，必须共同一致的传播或状态关系则在同一原子边界提交。批量工作不自动意味着整批成败；已经确认且保持一致的有效进展及时保存，未完成部分由后续运行根据权威状态重新推导。

批处理先依据真实领域关系建立最大相关范围和稳定自然顺序，再在范围内部按外部容量约束切分。语义原子保持完整，相关上下文尽量保留；容量利用兼顾固定调用成本，但不通过拼接无关内容或无依据地切碎相关内容来追求表面满载。并发可以改变完成时间，不能改变自然顺序、代表选择、分组结果或提交顺序。

```text
[当前行为]
     |
     | 后续行为必须继续使用的事实
     v
[持久化交接]
     |
     v
[后续行为]
```

每项持久化事实都应能够回答：

1. 谁负责写入；
2. 谁负责读取；
3. 它支持什么可观察行为；
4. 哪个位置是权威来源；
5. 它何时创建、更新和失效。

能够从权威事实稳定推导的数据在使用时计算。共享存储可以承载其他领域提供的信息，但解释权仍属于该领域。数据结构随已经确认的纵向切片小步演进，以真实读写关系证明每个字段存在的价值。

持久化结构优先直接表达领域资产及其真实读取、更新和事务边界。共同构成语义整体并经常一起消费的事实保持可见关联；需要独立继承、校验和写回的叶子保留精确身份。只有出现真实的跨领域统一读写需求时，才引入通用中间模型，避免以更通用但表达力更弱的结构替代清晰的领域关系。

## 9. 性能、并发与资源治理

性能属于用户可观察结果的一部分。性能目标从真实场景、代表性数据规模和明确瓶颈推导，并通过测量、分析或基准测试验证；不以未经证明的性能猜测增加实现复杂度。

并发方式根据工作负载选择：高并发等待型 I/O 可以使用异步任务，CPU 密集型工作可以使用受控的线程或并行计算；阻塞操作不得无边界占用异步执行器。同步或串行实现已经满足目标时，保持更简单的实现。

当某项共享根能力已经确认会承载广泛且高并发的 I/O 或 CPU 负载时，并发模型从第一个真实消费者开始就是契约的一部分：等待型 I/O 提供可背压的异步调用，CPU 工作进入独立的有界执行资源，活动量和队列预算由外部配置。根接口表达调用承诺，不把线程池、worker 或连接管理细节扩散给业务模块。

异步和多线程设计必须明确并发上限、背压、超时、取消、错误传播、任务生命周期及内存、文件句柄和外部服务配额等资源边界。并发不得破坏结果确定性、持久化一致性、错误语义和已有外部契约。

性能优化应先证明瓶颈，再针对瓶颈实施，并使用代表性负载验证优化收益、正确性和资源消耗；无法量化收益的复杂化不作为优化完成。

真实材料验证是一种按需采用的工作方式，不是每次任务都必须执行的固定流程。当静态分析、常规测试或小型夹具不足以确认真实性能、并发能力、资源消耗或复杂输入行为时，可以针对本机当前测试集临时编写不纳入版本控制的脚本或测试代码，在贴近真实场景的条件下取得证据。此类验证代码只需在测试集结构和内容相对稳定时具备适度复用性；测试集增删或变化后可以直接调整或重写，不为它建立长期兼容、迁移或通用框架。是否采用以及验证到何种程度，由当前风险、疑点和预期收益决定。

## 10. 实现、验证与完成定义

当前版本的用户可观察契约、持久数据定义和现实外部消费者决定本次改动必须同步更新的范围，但不产生对旧版本的兼容责任。版本迭代一经确认，旧契约、旧数据格式和旧消费者即退出现行范围，不得因其曾经存在而保留兼容、识别、拒绝、迁移或提示逻辑。既有结构无法准确表达当前语义、制造重复知识或持续阻碍实现时，采用范围明确的破坏性重构，并在同一次改动中更新全部当前消费者、测试、配置、现行规格和依赖图，彻底删除旧实现，不以转接层、双轨模型或历史专用错误保存已经失效的设计。

依赖树继续向下推进时，所有仍包含领域判断、状态转换或编排规则的非根能力都应获得真实实现；最终根接口只承载文件、计算、存储、网络、时间和运行时等环境机制。组合根统一负责外部配置转换、具体实现构造和生命周期管理。非根业务算法成立、根接口已经定义、根适配器真实可用和生产装配贯通是不同完成层级，交付时分别说明。

实现顺序与设计顺序保持一致：

1. 用高层行为固定当前目标的可观察结果；
2. 从消费方需要定义直接依赖的最小契约；
3. 实现并验证当前目标，范围外依赖使用测试替身；
4. 某个依赖进入实现范围后，再以其上游契约为起点实现并验证；
5. 端到端范围要求成立时，用最小真实样本贯通纵向路径；
6. 共享能力出现多个消费方后，验证各方依赖的是同一语义。

依赖暂未实现时，可以使用测试替身验证当前目标；生产装配只连接已经具备真实实现并完成相应验证的能力。契约存在、局部测试通过和底层组件完成都不单独代表纵向切片已经完成。

当前任务完成，意味着约定范围内的结果或上游承诺已经成立，失败语义明确，并完成相应验证。完整纵向切片完成，则进一步要求用户结果在真实装配下成立，必要状态能够正确交接，相关副作用和资源边界已经验证。未实现、未接线和未验证的范围在代码、文档和交付说明中保持可见。

架构图、现行规格和测试随已落地事实同步更新。图只描述真实依赖或明确标注的候选设计，不把设想呈现为现状。

## 11. 每次任务的方向检查

设计、实现和评审都使用以下问题校正方向：

1. 当前工作最终服务什么用户结果或上游承诺；
2. 谁拥有该结果及其中的规则和不变量；
3. 当前目标的直接依赖分别是什么，为什么是直接依赖；
4. 是否存在无语义增值的转发层，或绕过语义所有者的调用；
5. 新模块抽取的是稳定职责还是未经证明的未来设想；
6. 编写具体实现前是否已审计现有完整能力和局部职责，并说明哪些直接复用、哪些提炼共享、哪些因语义不同而保持独立；
7. 候选拆分是否具有独立职责和现实收益，还是只让文件更短、目录更整齐或层级更对称；拆分后是否反而产生转发层、碎片化、公开面或依赖膨胀；
8. 外部事实在哪个边界成为受信内部数据；
9. 新增状态是否有明确的写入方、读取方和行为用途；
10. 哪些证据能够证明纵向结果，而不只是证明局部代码可运行；
11. 当前负载是否存在经过测量的性能瓶颈，并发方式、资源上限及验证证据是否与真实瓶颈匹配；
12. 内部模型是否用强类型直接表达不变量，外部格式是否保持最小且易于大规模书写；
13. 每项有意义的策略选择是否来自外部受信配置，固定领域事实是否仍由代码契约拥有；
14. 批处理、验收和提交粒度是否分别符合领域关联、成本与一致性要求；
15. 新增领域事实是否已经由真实样本、权威说明或实际消费验证。
16. 版本迭代后是否已经从生产代码、测试、夹具、配置、现行规格、注释、错误和诊断中删除旧版本的全部痕迹，并确保不符合当前规范的输入只按普通无效输入处理。

这些问题尚不能回答时，先收束目标和边界，再扩大实现范围。任何技术选择都应能够沿依赖链回溯到真实需求。

## 12. 事实源与历史材料

本文件保存长期方向和判断方法；现行规格保存已经确认的产品与接口设计；代码和测试保存已经实现的行为；验证记录保存能力成立的证据。

领域模型、自然顺序、关联范围和边界行为以代表性真实材料、权威说明和实际读写关系共同验证。示例和测试夹具用于解释已经确认的事实，不能凭空创造字段、关系或命名再反向塑造领域模型。需要定位、继承或写回的原始字段、精确来源和未经授权的原值保持可追溯；语义化类型、注释和高层视图用于补充理解，不覆盖权威事实。

版本控制历史是当前系统旧版本材料唯一允许存在的位置；当前工作树的任何目录，包括 `archive/`，都不得保留当前系统的旧实现、旧协议、旧格式、旧配置、旧测试或兼容方式，也不得引用、加载、编译、识别或验证这些内容。`archive/` 只保存与当前系统版本无关的领域原始材料，用于验证当前领域事实和识别失败模式，不得成为当前系统旧版本的存放处。历史中仍然适用于当前实现的领域知识必须按当前规范重新表达和验证后才能进入当前项目。

只有长期方向、架构判断方法或项目边界发生变化时才更新本文件。当前参数、临时命名、实现进度和候选细节进入对应的现行规格或代码文档。

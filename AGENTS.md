# AGENTS.md

本文件定义当前项目长期有效的产品形态、架构方向和开发方法。通用工程、验证与交付要求沿用上层规则；项目中的现行代码、测试和文档负责补充已经落地的具体契约。

> 总纲：从每个游戏引擎的用户结果出发，自上而下形成独立纵向切片；在实现过程中，把职责清晰、语义稳定且具有现实复用性的能力提炼为模块，让不同切片按需组合成能力网络。

## 1. 一个产品入口，多个独立引擎命令域

项目以 Rust 实现，对外发布一个 `att` 可执行程序。根入口负责进程级生命周期、引擎命名空间选择、顶层帮助与版本信息，并把控制权交给目标引擎。

每个引擎完整拥有自己的 CLI 命令树，包括命令层级、参数、默认值、帮助文本、输入解析与校验、输出、错误语义和对应应用用例。完整命令路径是 CLI 契约身份的一部分；不同引擎中的命令即使使用相同词汇，也分别形成独立契约并独立演进。

```text
att（统一可执行入口）
├─ mz <mz-command> [mz-options]
│    └─ MZ 命令域：自己的命令、参数和纵向切片
├─ unity <unity-command> [unity-options]
│    └─ Unity 命令域：自己的命令、参数和纵向切片
├─ mv <mv-command> [mv-options]
└─ ue5 <ue5-command> [ue5-options]

同名命令属于不同契约：
att mz init       -> [MZ 初始化命令契约]
att unity init    -> [Unity 初始化命令契约]
```

真正的全局选项由根入口直接消费，并在所有引擎下保持相同语义。引擎选择完成后，其余参数、帮助和错误由对应引擎命令域解释。

每个纵向切片以一条完整的引擎命令路径为入口，从该命令的用户意图贯穿到可观察结果。跨引擎复用发生在命令契约之下：先保持命令独立，再把已经证明语义一致的下层职责提炼为共享能力。

```text
att mz <command>                    att unity <command>
        |                                    |
        v                                    v
  [MZ 命令契约]                       [Unity 命令契约]
        |                                    |
        v                                    v
  [MZ 应用用例] -----> [语义一致的共享能力] <----- [Unity 应用用例]
```

## 2. 当前产品切片：ATT MZ

当前首先完成 `att mz` 命令域。初始化、提取、翻译和写回分别作为独立 CLI 命令切片设计；它们通过持久化项目状态协作，共同构成 MZ 当前工作流。命令的最终名称和参数由各切片自己的用户需求确定。

1. 初始化切片：建立项目状态，创建当前流程需要的最小数据结构，并记录项目元数据。
2. 提取切片：汇集引擎固有位置、用户规则和脚本发现的文本，保存原文以及后续写回所需的信息。
3. 翻译切片：读取待翻译文本，组成有界任务块，以受控并发调用外部翻译能力，在响应边界完成验收并保存译文。
4. 写回切片：读取已验收译文，由 MZ 语义解释定位并修改目标内容。

```text
att mz <初始化命令>  ----写入----> [项目持久状态]
att mz <提取命令>    <---读写---> [项目持久状态]
att mz <翻译命令>    <---读写---> [项目持久状态]
att mz <写回命令>    <---读取---- [项目持久状态]
        |
        +----产生----> [更新后的游戏内容]

每一行都是独立 CLI 契约；图中的“项目持久状态”是同一个最小事实交接点。
```

解包、封包等步骤由具体引擎在真实输入格式需要时提出。当前 MZ 切片优先覆盖能够直接处理的明文数据；以后出现新的输入形态时，再为相应命令增加所需能力。

MZ 的命令和阶段只描述 MZ 当前产品范围。其他引擎根据自己的用户流程决定命令、参数、阶段和顺序。

## 3. 自上而下：由用户结果生成依赖

每个编程任务从一条完整命令路径及其用户可观察结果开始。编码前先明确：

1. 命令入口和用户意图；
2. 成功结果、失败结果和需要保持的行为；
3. 本次纵向边界以及明确留给后续的内容；
4. 对该结果负责的最高层用例。

最高层用例决定它需要哪些直接依赖，并从调用方视角定义这些依赖的最小契约。契约表达调用方需要提交什么、获得什么以及如何理解失败；具体实现细节留在依赖模块内部。

```text
[完整命令路径]
        |
        v
[用户可观察结果]
        |
        v
[最高层用例]
        |
        +----> [直接依赖契约]
        +----> [直接依赖契约]
        |
        v
[实现并验证当前用例]

后续：让一个尚未实现的依赖成为新的最高层目标，重复同一过程。
```

接口先行，是先定义上层真正需要的承诺。依赖不在本次实现范围时，用测试替身验证最高层行为，并把生产能力保持为明确的未接线状态；任务要求端到端完成时，沿同一依赖图继续逐层实现。生产组合只注册已经具备真实实现并完成验证的命令。

接口由消费方在语义上拥有，物理上可以放入被用例和实现共同依赖的窄契约模块。表达接口时采用满足当前契约的最简单形式，让目录、模块、crate 和基础设施成为用例推导的结果。

## 4. 模块化：从职责形成可复用能力网络

模块围绕一个可命名的职责形成，把内部状态、算法、第三方细节和复杂流程留在边界内，对外提供能够直接表达调用方意图的高级接口。

每次新增代码时都判断：

- 它是否表达一个独立职责；
- 哪些知识属于当前引擎或当前命令；
- 哪些语义能够服务现在或可明确指出的后续消费方；
- 提炼边界后是否能够减少重复知识并简化调用方。

独立职责先在所属切片中形成边界清晰的模块。当相同语义能够服务当前消费方或可明确指出的后续消费方时，将其提炼为共享模块；后续真实消费负责验证并修正边界。需要独立依赖、编译隔离、测试边界或发布边界时，再提升为独立 crate。

```text
独立且可命名的职责
        |
        v
切片内私有模块
        |  相同语义被其他切片需要
        v
共享能力模块
        |  需要独立依赖、编译或发布边界
        v
独立 crate
```

共享模块组成按需连接的能力网络，而不是所有引擎都要经过的固定流水线。每个引擎切片保留自己的命令契约和编排权。

```text
[引擎切片一] --+--> [共享能力 α]
               +--> [共享能力 β]
               +--> [切片一私有能力]

[引擎切片二] --+--> [共享能力 α]
               +--> [共享能力 γ]
               +--> [切片二私有能力]

各切片选择现实需要的能力；共享节点只承载一致语义。
```

围绕同一职责、共享同一组内部实现并适合一起编译和发布的 `mod` 放入同一个 crate。文件大小是复核职责的信号，职责、变化原因和依赖方向才决定真实边界。公开面随真实调用方增长，保持高级、最小和稳定。

## 5. 边界负责建立信任，内部按契约协作

外部数据由最了解其语义的边界完成解析和校验，并转换成能够承载不变量的内部类型。每项不变量都有一个明确责任方；转换成功后，内部模块直接传递和使用受信类型。

```text
外部数据
   |
   v
[边界责任方：解析 + 校验]
   |
   v
[受信内部类型]
   |
   +----> [内部模块] ----> [内部模块] ----> [内部模块]
                                                |
                                                v
                               [副作用责任方：重观测动态外部事实]
```

静态语义在所属边界建立一次；文件身份、写入权限、并发状态等会随时间变化的事实，由执行副作用的模块在操作发生前重新观测。运行时失败按原义向上传播，内部不变量被破坏时以明确错误暴露程序缺陷。

外部服务和可执行扩展同样拥有清晰边界：调用方定义请求与结果语义，边界负责协议转换、信任模型和资源范围，应用用例负责业务验收。通过边界进入内部的结果继续按同一契约流转。

## 6. 持久化由命令切片之间的交接产生

数据库保存纵向切片跨命令、跨进程继续工作所需的最小事实。数据模型从真实交接需求产生，并随已经完成的切片小步演进。

```text
[命令切片 N]
      |
      | 后续命令继续工作所需的最小事实
      v
[持久化交接]
      |
      v
[命令切片 N+1]

字段成立条件 = 当前写入方 + 当前读取方 + 用户可观察用途
```

每项持久化数据都有明确写入方、读取方和对应的用户行为。能够从权威事实稳定推导的数据在使用时计算；确需跨阶段或跨进程保留的数据进入持久化模型。数据库结构、版本和字段定义各有唯一责任方。

跨引擎保持一致的翻译事实可以进入共享模型。定位、格式和项目结构等引擎知识由相应引擎切片拥有；共享持久化可以原样保存引擎提供的信息，但解释权仍属于该引擎。需要写回的信息能够完成“提取、退出、重新启动、写回”的往返。

数据库是翻译工作流中持久状态的权威记录；游戏文件是外部内容事实和写回目标。执行写回的模块在产生副作用前确认当前目标仍与项目状态相匹配。

## 7. 实现、验证与交付

实现顺序与设计顺序一致：

1. 用高层行为测试固定完整命令路径的用户结果；
2. 用边界契约测试验证具体依赖实现；
3. 用最小真实样本验证命令的完整纵向切片；
4. 当共享能力出现多个真实消费方时，补充跨切片契约验证。

不同引擎的同名命令分别验证参数、默认值、帮助、输出和错误语义。共享能力的测试只覆盖共享承诺，不代替各引擎命令自己的行为测试。

涉及并发和外部服务时，用例明确任务边界、资源上限和失败语义，并让这些约束真实参与执行。生产装配只连接已经实现的能力；尚未实现的依赖在状态说明、测试和交付中保持可见。

每次交付说明：

1. 完成了哪条命令路径及其用户结果；
2. 哪些依赖已经实现，哪些仍只有契约；
3. 哪些职责属于引擎切片，哪些已经提炼为共享模块；
4. 外部数据在哪个边界成为受信内部数据；
5. 新增持久化事实如何连接当前命令切片；
6. 实际执行了哪些验证，以及仍未验证的范围。

根目录中的现行代码、测试和文档是当前事实源。`archive/` 提供历史证据和领域知识；其中的信息经过当前纵向切片重新验证后，才进入现行设计。
